# D3 缺口登记(batch-e0)

已知缺口逐条:**补法 / 成本 / 不补时的论文措辞**。最后一列是合同——若某缺口不补,论文
就照那一行写,不得改成更强的说法。

配套文件:`docs/eval/evidence_ledger.md`(数字与出处)、`docs/eval/metric_contract.md`(指标定义)。

---

## 1. needs-recompute 清单 —— 它决定 E2 的运行矩阵

台账中状态为 `needs-recompute` 或 `需三跑` 的条目,按"是否需要新的 API 支出"分两组。

### 1a. 不需要任何 API 支出(可在 E1 内做完)

| 台账号 | 要重算什么 | 重算来源 | 备注 |
| --- | --- | --- | --- |
| B-02 | LOPO 逐折矩阵与 holdout 一致率 | 确定性:`babeldoc/magazine/page_classifier.py` + `configs/page_types.json` + `corpus/page_labels.json`,按刊物留一折 | 原 0.938 是 **v1 语料 + 调参会话折划分**的产物,两者都已不存在;只能产出 **v2 新数字**,不能"恢复"0.938。**batch-e1.2 已做**:`docs/eval/results_e1/lopo_v2.json`(台账 B-11),`refit_per_fold=false` |
| C-04 | VLM 的 policy 级一致率(现有产物只有 kind 与 label_set_coverage) | 前三档模型的回复已全部在缓存(`cache_hits=18`),经 `tools/vlm_classify_eval.py` 补算 policy 列即可 | `gpt-5.6-terra` 那一档 `cache_hits=0 / requests=18`。**batch-e1.2 已做且四档齐全**:policy 列由冻结报告**离线**重算(`--from-report`),连缓存都不必查,故第四档同样零成本、同源;见 `docs/eval/results_e1/vlm_policy_c04.json`。**batch-e2.2 复跑该重算,二次运行逐字节相同**(9 716 bytes),并核实"terra 18 次同口径补算"不可执行(见 GAP-10 与台账 C-06) |
| M6 / M7 / M8 / M9 | policy 一致率、image-area delta、page-count delta、image IoU | 冻结的 `checkpoint.11_typesetting` 与 `examples/baseline/` 的六份 PDF | 纯几何,**E1 就该算完,不进 E2 运行矩阵**。**batch-e1.2 已做**:`docs/eval/results_e1/eval_corpus.json`;M4/M5 的上游列按台账 G-07 记 not-comparable |

### 1b. 需要新的运行(E2 运行矩阵)

| 台账号 | 要重算什么 | 运行 | 成本估算(依据) |
| --- | --- | --- | --- |
| A-12 / A-13 | 邻段漂移的归因(重组效应 vs 引擎重复噪声) | **R1 三跑设计**:Courier-en `chain_off` ×2(独立采样)+ `chain_on` ×1(走缓存) | 估算:约 **90** 次调用、**5~6 万** prompt tokens、**约 13 分钟**。**batch-e2.1 已做**,实测 **124** 次调用、**105 759** prompt tokens、**245 s**(台账 A-21);`chain_on` 的 54 次翻译请求全部命中缓存。归因结果见台账 A-18,GAP-01 已关账 |
| M10 | GEMBA-MQM 拼接点标注 | **R2 判官跑**:全语料 35 条裁定边界 ×(chain_on / chain_off)两臂 | ≤ **70** 次判官请求;单价依 judge 模型而定(见 GAP-03)。必须经统一缓存客户端,首跑后可逐字节重放。**batch-e2.2 已做**:点集收窄为**裁定单全部 5 条 link 正样本 × 各自现存臂 = 14 行**,实测 **14** 次请求(13 个不同 prompt)、16 256 + 6 806 tokens、**0** 拒答;协议 `docs/eval/splice_protocol.md`,结果台账 A-22~A-24。**30 条负边界未跑**(预算内可跑,见 GAP-03 的遗留) |
| E-09 | `escalation_surfacing` 的活证据 | **R3(可选)**:构造一条长到触发 `max_tokens=2048` 截断的链,使其走 `translation_unavailable` 逃生 | 1 份合成文档、数次调用。**若不做,按 GAP-04 的措辞写**。**batch-e2.2 裁定不做**:按 GAP-04 的合成覆盖声明收口,台账 E-09 的状态由 `needs-recompute` 改为 `直接可引(只能按声明形式引)` |

**E2 运行矩阵结论:R1 必做(它是唯一能把 A-12 从"需三跑"降为可断言的路径),R2 必做但
须先定 judge 模型,R3 可选。除此之外 E2 不需要任何新翻译运行——其余全部指标都能在
已冻结的 checkpoint 与 PDF 上算。**

---

## 2. 缺口逐条

### GAP-01 邻段漂移的归因需三跑设计 —— 已由 batch-e2.1 的 R1 三跑关账

> **已补。** 三跑产物在 `examples/output/e2/r1/`(工作区,sha256 登记在
> `docs/eval/results_e2/drift_attribution.json` 的 `runs`),归因表在
> `docs/eval/results_e2/drift_attribution.md`,结论进台账 A-18、A-19,方法学副产物进
> G-09。A-12 的状态由"需三跑"改为"直接可引"。

- **原现状**:b5.3 在 Courier-en 上匹配 123 段、15 段变(4 链成员 + 11 邻段),同时测得
  `gpt-4o` 在 `temperature=0` 下同一 prompt 三答、相似度 0.989 / 0.995(台账 A-12、G-01)。
  11 处邻段变化的量级与词级重采样噪声同量级;而 prompt 未变的段落是**从缓存取回的,
  因此按构造相同而非按测量相同**,该设计无法分辨两者。
- **补法(已执行)**:两个 `chain_off` 臂各带一个自己的 cache impact 参数,因此读不到对方的
  答案而线上请求逐字节相同——独立采样与冻结重放两者兼得(登记为 W-E2-01);`chain_on` 走共享
  命名空间冻结重放。前提逐项校验通过:三臂翻译前的文档逐段相同,两个 off 臂的**批次组成
  逐段相同**。
- **结论**:
  1. 同一配置的跑间方差是 **43/132 = 0.3258**(整段是否逐字节相同);
  2. **27** 段落在被重组的批次里,页 2/3/7/8,与 A-13 的机制逐页吻合;
  3. 既变更又落在重组批次里的段落恰 **15** 段(4 成员 + 11 邻段),与 A-12 逐数吻合——
     这正是 b5.3 的共享缓存设计能看见的全集;
  4. 11 段邻段中 **8 段归重组**(两次独立采样一致、批次确实移动),**3 段与重复采样不可
     分辨**(两次独立采样本身就不同)。
- **可引措辞**(取代原"不补的措辞"):
  > 链级联合翻译在被重组的页上同时改变了链成员与其同批邻段的译文。三跑设计(同一配置
  > 独立采样两次,加一次开关翻转)把这些邻段变化分开:十一处邻段变化中八处在两次独立
  > 采样下取值一致而仅在开关翻转时改变,归于批次重组;其余三处在两次独立采样之间本身
  > 就不同,与本引擎的重复采样噪声不可分辨。该配置的跑间方差实测为 0.326(132 段中 43 段
  > 逐字节不同),是一切单跑差异的解释上限。结论限于单样张单模型,不作显著性主张。
- **未尽处**:`chain_on` 臂是冻结重放,即另一场合的一次抽样,因此"两次 off 一致而 on 不同"
  在批次未移动时并不蕴含机制效应(实测有 11 段属此)。归因表因此并列两列:`gap01` 列按本
  缺口原文的三列规则计算、可由三列译文复算,`verdict` 列加入批次证据。论文引用后者。

### GAP-02 LOPO 逐折矩阵没有产物

- **现状**:唯一出处是 `plans/PLAN_B2_7.md`:22 对调参会话的转述("0.903 整体 / 0.938 holdout")。
  逐折矩阵、折划分、调参轨迹均未落盘;调参所用的 v1 语料两条样张已在 batch-b7.5.1 被替换。
- **补法**:1a 中的 B-02 行——在 v2 语料上按刊物留一重做 LOPO,产出新的逐折矩阵并入库。
- **成本**:零 API,确定性,E1 内完成。
- **batch-e1.2 落地**:矩阵已入库(`docs/eval/results_e1/lopo_v2.json`,台账 B-11,二次运行逐位
  相等)。但**本缺口不因此关闭**:本仓库没有调参器,任何折都不重新拟合
  (`protocol.refit_per_fold=false`),且词表调参时全部 v2 语料在场,故矩阵是**组合性**的,
  不是留出估计。下面的"不补的措辞"**继续有效**,只在其上追加一句:逐折分布已落盘可查
  (held-out kind 7/9、3/4、8/9、10/16、3/3)。
- **不补的措辞**(且 0.938 一律**不得出现在论文中**):
  > 泛化性以按刊物留一交叉验证的方式配置,所有自适应组件在配置时不接触留出刊物
  > (背景章 §2.7)。本文报告的是全语料一致率 0.879 (29/33)(v2 语料);逐折数字在语料
  > 换血中未予保留,故不作留出集主张。

### GAP-03 GEMBA-MQM 判官的同源偏倚 —— 已由 batch-e2.2 按方案 (a) 关账

> **已补(异族判官 + 缓存钉版 + 人工抽验)。** 判官取 `gpt-5.6-terra`,与被测 `gpt-4o`
> 非同族;协议 `docs/eval/splice_protocol.md`;逐点结果 `docs/eval/results_e2/splice_judgements.json`
> (5 点 / 14 行 / 0 拒答),每行钉住判官模型与传输参数;全部回复经统一缓存客户端落盘,
> `--offline` 重放 14/14 命中、报告逐字节相同。人工抽验清单
> `docs/eval/results_e2/splice_manual_review.json` 覆盖 **14/14 = 100%**(语料主人裁定取
> 全覆盖而非建议的 20%)。结论进台账 A-22~A-26。
>
> **裁决已入库**,方案 (a) 的第二半因此有数:裁决把 6 行判为 `PROTOCOL-INVALID`(窗口没有
> 对准被裁定的切断,见 GAP-14),有效集 **8 行 / 3 点**,判官与人工在有效集上一致
> **6/8**;两处不一致一为高估(GAP-16),一为漏报(GAP-15 与 GAP-17)。裁决另开出五条缺口
> GAP-14~GAP-18。
>
> **两处未尽,一并登记**:(1) 点集只有 5 条 link 正样本,**无负控**——30 条 `link: false`
> 边界未跑(预算内可跑:14 + 60 ≤ 70),故本次结果只支持"在被裁定切断的边界上判官报出了
> 什么",不支持"判官能分辨好坏拼接";(2) 一致率的分母是 8 行、每行一次抽样,**判官结果
> 仍只作定性错误类型提示**,与方案 (a) 的原始承诺一致。

- **原现状**:被测系统用 `gpt-4o`。若判官也用 `gpt-4o`,判官与被测同源,系统性偏向自身输出
  的风险无法排除;kocmi2023 自己给出的告诫正是对专有黑盒的依赖(背景章 §2.7)。
- **补法二选一(用户决策)**:
  - **(a) 异源判官**:判官改用非 `gpt-4o` 族模型;偏倚论证成立,代价是判官本身的质量需另行说明。
  - **(b) 同源 + 声明 + 人工抽验**:沿用 `gpt-4o` 判官,在论文中显式声明同源偏倚,
    并对 ≥20% 的拼接点做人工复核,报告判官与人工的一致率。
- **成本**:(a) 与 (b) 的 API 成本相同(≤70 次);(b) 额外约 7 个拼接点的人工工作量。
- **不补的措辞**(即两条都不做时):
  > 拼接点标注由一个与被测系统同族的大模型给出,判官与被测系统同源,其结果**只作定性
  > 错误类型的提示,不作为质量排序的依据**;全部判官回复已缓存并可逐字节重放。

### GAP-04 `escalation_surfacing` 零活证据 —— batch-e2.2 裁定按声明收口

> **不补,按下面的"不补的措辞"写。** R3 不跑是用户裁定;台账 E-09 的状态因此由
> `needs-recompute` 改为 `直接可引`,但左列写死它**只能按合成覆盖声明的形式引**。
> 这不是把缺口填上,是把它从"待重算的数字"改记为"已定的措辞":12 次零触发本身有出处
> 且本会话已复核,缺的是一次真触发,而那要靠 R3。

- **现状**:b8.3 与 b8.4 合计 **12 次真跑**,`escalation_surfacing` 一次未触发(台账 E-09)。
  该检测器至今只被 b8.1 的 fixture 与门禁的合成场景运行过。同类情况还有
  `converged_with_residuals`(b8.4 §6,同样只有合成场景)。
- **补法**:上表 R3——构造一条超过 `max_tokens=2048` 的链,让它走 `translation_unavailable`
  逃生路径,从而生成真实的 escalation 记录。
- **成本**:1 份合成文档、数次调用;实现代价小于运行代价。
- **不补的措辞**(推荐直接采用,合成覆盖声明):
  > `escalation_surfacing` 与 `converged_with_residuals` 两条路径由合成场景覆盖并在门禁中
  > 逐次断言,**在真实语料的十二次运行中均未被触发**。本文因此把它们记为"机制已实现且
  > 有合成覆盖",而不记为"已被文档验证"。

### GAP-05 "v1 修复零落地"的三段式措辞 —— 已被 batch-b8.4 推翻,须重述

> **与 PLAN_E0 前提不符,现场记录。** PLAN_E0 D3 把"v1 修复零落地的三段式措辞"列为
> "已定,登记为合同"。该前提在 batch-b8.3 时成立(6/6 conserved、0 段修复,台账 E-03),
> 但 **batch-b8.4 有三处修复进入成品 PDF 且盒不变**(台账 E-06,本会话已从
> `examples/output/b8_4/smoke/evidence.json` 现场复核 `box_held: true` ×3)。E0 是盘点批次,
> 因此按事实登记而非转录旧措辞。

- **重述后的三段式**(建议作为新合同):
  1. **机制成立**:检测 → 决策 → 带裁决送出 → 写回 → 增量重排 → 复检的完整链路在真实
     语料上跑通,**三处修复进入成品 PDF**(CERNCourier-en `p2#32`、FD-en-v2 `p5#14` 与 `p5#9`),
     每一处的段落盒保持不变。
  2. **主角缺陷被拒,且拒因是结构性的**:B8 线全程的主角 `p6#15` 在被正确读序、正确翻译、
     带上人工裁决的刊名之后,仍在写回阶段被拒——排版层没有竖排模式,重排该行需要比原
     竖条更大的面积(746 pt² → 69 700 pt²)。这是能力边界而非实现缺陷,守卫拦下了一次
     会毁版的"成功"。
  3. **一处结构性不可达**:`p1#9` 是译者已渲染过的乱序标题,v1 动作词表中无一条能回答它,
     且为其写的裁决条目匹配不到任何 prompt。
- **成本**:零(纯措辞)。
- **不补的后果**:论文会写出一个被自己产物推翻的说法。**必须补。**

### GAP-06 两处计数口径与 commit message 的措辞出入 —— batch-e2.2 已落台账

> **已补(纯措辞)。** 统一口径已写进台账 E-12 的"可引形式"一栏:一律引全部 findings 的
> **19 / 25 / 32**;引 b8.3 §7 那一句必须加注"该处只计 residue,故写作 19 / 24 / 28";
> commit message 不改写历史,引报告而非 commit。E-12 的状态由 `需重述` 改为 `直接可引`。

- **现状**(台账 E-12):
  - b8.3 报告 §7 写决策模型"在报告 19、24、28 条 findings 的文档上仍只命名三条",数的是
    **residue 一种**;b8.4 §5 写"19、25、32",数的是**全部 findings**。两者各自内部自洽,
    但并排引用会被读成矛盾。
  - b8.4 的 commit message 只有一行 "batch b8.4: reading order, treated semantics, landed repair",
    未载主角 `p6#15` 被拒——而报告的第一句就是那个拒绝。
- **补法**:引用前统一为**全部 findings** 口径(19 / 25 / 32),并在引用 b8.3 那句时加注
  "该处只计 residue"。commit message 不改写历史。
- **成本**:零。
- **不补的措辞**:不适用——这不是可以留着的缺口,是引用纪律。

### GAP-07 关键出处文件未入库(本台账最大的单点风险)—— 已由 batch-e1 会话一分级入库

> **已补(分级)。** 用户裁定的入库集已进 git:`docs/dissertation/background_chapter.tex`、
> `examples/baseline/manifest.json`、`examples/baseline/baseline.report.md`、
> `examples/baseline/logs/`、`examples/baseline/integrity/`、`examples/baseline/cache/cache.v1.db`、
> 四档消融的 `vlm_eval.report.json` 与 `vlm_eval.summary.txt`(见
> `examples/output/vlm_ablation/gpt-4o/vlm_eval.report.json` 及同级另三档)、
> 三份 sweep 日志(`examples/output/b4/run_all.b4_2.final.log`、
> `examples/output/b2_7/run_all.full.log`、`examples/output/run_all.b8_3.log`)。
> **六份基线成品 PDF 按体积不入库**,由 `spec_checks/spec_check_e0.py` 的工作区档以显式
> "路径 + sha256"清单断言其存续。台账 C 组与 F 组的**入库**列因此已从 `worktree` 改为 `git`。

- **原现状**:下列出处**只存在于工作区**,不在任何 tag 里,一次 clone 或一次
  `tools/prune_outputs.py` 就可能失去:
  - `examples/baseline/` 整目录(六份上游基线 PDF、manifest、日志、缓存冻结拷贝、完整性快照)
    —— 台账 F 全组、G-02 的唯一出处。
  - `examples/output/vlm_ablation/`(四档模型的消融产物)—— 台账 C 全组的唯一出处。
  - `examples/output/b4/run_all.b4_2.final.log`、`examples/output/b2_7/run_all.full.log`、
    `examples/output/run_all.b8_3.log` 等 sweep 日志 —— 台账 A-01、B-01、B-05、B-06 的 witness。
  - `docs/dissertation/background_chapter.tex` —— **D2 全部 eq 标签的唯一解析对象**。
- **补法**:由用户决定各自入库与否(这些是用户侧资产,E0 的白名单不含它们)。
  可入库的优先级:`background_chapter.tex` > `vlm_ablation` 的两个 json/txt >
  `baseline/manifest.json` 与 `baseline.report.md` > 基线 PDF(体积大,可另行归档)。
- **成本**:纯仓库体积;`vlm_ablation` 的 json/txt 与 baseline 的 manifest/report 都是小文本。
- **不补的措辞**:不适用——这不是论文措辞问题,是证据可存续性问题。
  **若不入库,台账 C 与 F 两组的状态应从"直接可引"降为"引用前须现场复核存在"。**

### GAP-08 五缺陷家族 → E3 对照轴的映射表

上游基线报告归纳的五个反复出现的缺陷家族(台账 F-05)是 E3 对照的天然轴。映射如下,
每一轴给出**已有的量**与**E3 需要新建的量**。

| 轴 | 缺陷家族(F-05) | 上游实例 | 已有的量 | E3 需新建 |
| --- | --- | --- | --- | --- |
| **A 残留与叠印** | 1. 未擦除原文 | AramcoWorld p5 三层文字叠印、p9 英文尾巴 | `untranslated_residue` 检出(台账 E-10、E-01) | M4 Overlap 差值、M9 image IoU;**须在上游侧也跑一遍同一检测器** |
| **B 首字下沉** | 2. 首字下沉 | Courier-en p4/p5 碰撞、p7 未译拉丁首字、CERN p3 断连字 | HITL 的 `dropCapDecision` 三条裁决(`examples/output/b7_3/smoke.report.md` §5) | **消费端已于 batch-b9.4 落地**(`babeldoc/magazine/drop_cap.py` 的 `apply`,开关 `magazine_drop_cap_apply`,默认关):`flatten` 并首字入正文流,`keep` 原样,未裁决候选取目标语言声明的默认值。E3 的轴 B 因此可给出机器可测的量——裁决站点的首字号是否回落到本段正字号——而不再只能目视对照;下方"不补的措辞"随之作废 |
| **C furniture 覆盖** | 3. display 与 chrome 文本漏译 | 各刊 section chip、masthead、running title、旋转 credit;zh→en 尤重 | 刊名三态实录(台账 D-07);`fallback_line` 无 prompt 是已知结构原因 | 按 `layout_label` 分组的译出率;**分母口径须声明**(`fallback_line` 与 `abandon` 单列) |
| **D 文本流** | 4. 文本流损坏 | 断词(zh→en 尤重)、栏尾截断、孤立碎片、结构化列表塌成流水段 | `fragment_cluster` 检出 3 处(台账 E-10);链的零外溢证据(A-10) | **M1 mid-unit page-break rate 是这一轴的主指标**;断词率另需一个量 |
| **E 覆盖** | 5. 整块静默漏译 | Vogue p2(零警告)、FD p9 | 上游 fallback 警告矩阵(F-04)与"警告不追踪质量"的反例(F-06) | 段落级译出率(译文与源文相同的段落占比);可直接在 `examples/baseline/` 的六份 PDF 上算,**零 API** |

- **成本**:轴 A/C/D/E 的新量都是确定性的,可在 E1 实现、E3 使用;轴 B 自 batch-b9.4 起有自动路径。
- **原"不补的措辞"(仅对轴 B)已作废**,原文留档如下,论文不得再引:
  > ~~首字下沉的处置在本系统中由人工裁决点记录(`dropCapDecision`),但排版层尚未消费该
  > 裁决,故本轴以目视对照呈现,不给出自动指标。~~

  改写为:
  > 首字下沉的处置由人工裁决点记录(`dropCapDecision`),并由 `magazine_drop_cap_apply`
  > 消费:`flatten` 把放大的首字并回它所开启的正文,`keep` 保持原样,未裁决的候选取目标
  > 语言声明的默认值。故本轴的量为裁决站点上首字号相对本段正字号的回落。

### GAP-09 官方中文版原件与哈希已失效(台账 A-15)—— 已由 batch-e1 会话一重锚

> **已补(重锚而非重取)。** 官方中文版原件早已在树内,只是台账指错了对象:语料样张
> `examples/input/Courier-zh.pdf` 就是 UNESCO Courier 2026 年 1–3 月刊的官方中文版,
> `corpus/manifest.json` 载其 sha256 `2975c623b0bc604a4deb15f36229de512d295f69e171462f083b3c7d494bbaf1`,
> 且该条 notes 自述"Contains the official MID-WORD page split at zh p10->p11"。
> A-15 的出处已改锚到该样张与登记文件,状态由 `needs-recompute` 改为 `直接可引`。

- **原现状**:b5.3 §1d 记录了官方中文版的 sha256 `fa789f8a…46a7d3ac`,但那份产物
  `parallel/courier_official_zh.md` 已被保留策略淘汰(见台账"已失效产物登记"),
  该哈希无法现场核验。四方对照表的**引文本身**逐字保存在报告与
  `docs/dissertation_assets/dissertation_b5_evidence_section_filled.md` 中,可引。
- **遗留纪律**:旧 sha256 `fa789f8a…46a7d3ac` 描述的是被淘汰的**抽取文本**,不是 PDF 原件,
  两者不同物;论文一律引重锚后的样张哈希,旧值不得出现。
- **成本**:零 API,零外部取件。

### GAP-10 VLM "policy 级零增益"的措辞与产物不符 —— batch-e2.2 关账,且"18 次补算"作废

> **已补(离线重算),且原定的补法被证伪。** policy 列由 batch-e1.2 从四份冻结报告离线
> 重算(`docs/eval/results_e1/vlm_policy_c04.json`),batch-e2.2 复跑该重算、**二次运行
> 逐字节相同**(9 716 bytes,零 API)。
>
> **"`gpt-5.6-terra` 若要同口径需 18 次调用"这条本会话现场核实为不可执行**:那 18 个
> routed page 属于 v1 语料的 31 页,其中 **8 页**(`AramcoWorld-en.pdf` 5、`FD-en.pdf` 3)
> 落在 batch-b7.5.1 换血时移出的两份样张上,PDF 已不在 `examples/input/`。**不只是 terra,
> 四档都无法再实跑重现那个分母。** 因此"四档同源同口径"的正确说法是**离线重算**,不是
> 缓存重放;量化依据进台账 C-06,C-04 的左列已写明。用户裁定不另跑 v2 单档,以免制造一个
> 与冻结四档分母不同、却会被并排读的第五个数。

- **现状**:结论措辞写在 `CLAUDE.md`:30 与 `plans/PLAN_B4.md`:14("四档模型均无 policy 级
  增益"),但四份产物(`examples/output/vlm_ablation/gpt-4o/vlm_eval.report.json` 及同级
  另三档)只有 `agreement`
  (kind)与 `label_set_coverage` 两组,**没有 policy 列**(台账 C-04)。
- **补法**:1a 的 C-04 行——在现有缓存回复上补算 policy 列。
- **成本**:前三档零 API;`gpt-5.6-terra` 若要同口径需 18 次调用。
- **不补的措辞**:
  > 四档视觉模型在页面类型判定上的整体一致率分别为 0.9032 / 0.8387 / 0.9032 / 0.9032,
  > 均**不高于**确定性判定的 0.9032,故兜底保持关闭。该消融在页面类型口径上进行;
  > policy 口径未单独测量。

### GAP-11 上游版本对的措辞

- **现状**(台账 F-02):上游侧是 GitHub `main` 的 zip 下载,**没有 git 元数据**,故
  `git rev-parse HEAD` 无法执行;取而代之的是逐文件 `git hash-object` 比对:410 个文件中
  408 个与 fork 的上游基线 `17480db`(Release v0.6.4)逐字节相同,唯一差异是 `README.md`
  的两处链接改写。
- **补法**:无需补——证据比原本要求的检查更强。只是措辞必须准确。
- **合同措辞**(直接采用):
  > 对照的上游版本是 BabelDOC 0.6.4 分支的 `main`(略晚于 `v0.6.4` 标签)。两侧源码经逐
  > 文件哈希比对确认一致,唯一差异为 `README.md` 中两处文档链接,不涉及任何源码;
  > 上游工作树在整个基线批次前后的 415 个文件哈希完全一致,证明未被修改。

### GAP-12 样本规模是所有比率型主张的上限

- **现状**:全语料 6 样张 / 5 刊物 / 41 页 / 35 条裁定边界,其中 **link 仅 5 条**(台账 A-04、B-10);
  b5.3 冒烟时点更只有 2 条真实链(其中 1 条正文链)。b6.3 的 term consistency 只有 19 个合格词,
  且报告自陈"不构成主张"(台账 G-04)。
- **补法**:扩语料是唯一补法,属用户侧决策(`corpus/registry.user.json` 只有用户可编辑)。
- **成本**:每新增一刊需新的裁定工作量(页型 + 边界 + 术语)。
- **不补的措辞**(适用于全部链线与一致性结论):
  > 本文的链级结论建立在 5 条人工裁定的跨页语义切断样本之上,其中正文句中切断 3 条。
  > 相应主张限定为**存在性论证与机制验证**,不作比率型或显著性主张。

### GAP-13 A-09 的两半证据强度不同 —— 重述已落台账(batch-e2.1 开,batch-e2.2 关)

> **已补(纯措辞)。** 下面"合同措辞"一段已逐字写进台账 A-09 的数字栏与状态栏:可引的是
> **虚构收束**与**专利句被切成两半**两项,`the grass`→`草地` 一律不得引。A-09 的状态由
> `需重述` 改为 `直接可引`。
>
> **batch-e2.2 追加一项候选证据,不改本缺口的判定。** M10 判官在同一边界的四臂上都记
> `accuracy/mistranslation` critical,但对 `upstream` / `chain_off_1` / `chain_off_2` 三行
> 指的是**谓述层**的错误——专利被安在"草"上而不是"由草制成的复合材料"上——而不是
> `草地` 这个词。该判词在三处独立产物上出现、在 `chain_on` 上不出现(台账 A-23)。它是
> **第三项候选证据**,但来自单判官单次抽样,按 GAP-03 只作定性提示;GAP-13 撤下的那一项
> 仍然撤下,不因判官说了别的而复活。

- **现状**:台账 A-09 把 b5.3 §1b 的两处缺陷并列为一条"A/B 缺陷对":悬空从句被虚构收束
  (`…以及一种复合材料。`),以及先行词丢失致 `the grass` 被译为 `草地`(草坪)。R1 的两个
  独立 `chain_off` 臂同时复核了这两处:
  - **虚构收束复现**:两臂的 `p7#4` 整段并不相同(段中间有词级差异),但收尾那一句**逐字节
    相同**,都以 `以及一种复合材料。` 结束;`p8#8` 的开头两臂也逐字节相同
    (`这种草已经被申请了专利。`)。
  - **先行词丢失未复现**:两臂都把 `the grass` 译作 `这种草`,是对的。`草地` 是 b5.3 那一次
    抽样的产物。
- **补法**:纯措辞,零 API。切断损害的确定性证据改为两项:**虚构收束**,以及**专利句被切成
  两半**(off 臂把 `A composite material from the grass has been patented.` 拆成页 7 的
  "以及一种复合材料。"与页 8 的"这种草已经被申请了专利。"两句,内容重复且逻辑断裂)。
- **成本**:零。
- **不补的后果**:论文会拿一个落在采样方差里的误译当确定性缺陷。**必须补。**
- **合同措辞**:
  > 把一个句中被切断的片段单独送译,会让引擎为悬空从句虚构一个句末收束;该现象在两次
  > 独立采样下逐字节复现。同一处的另一个观察——先行词丢失导致的名词误译——在两次独立
  > 采样下未能复现,故本文不引作缺陷证据。

---

## 3. 人工裁决开出的缺口(batch-e2.2 追加,编号取自裁决文件 tally 节)

下列五条由 `docs/eval/results_e2/splice_manual_review.json` 的人工裁决开出。裁决文件是
**用户所有**,机器会话只读;工具遇到已填写的裁决不再覆写(`tools/splice_judge.py` 的
`is_ruled`)。

### GAP-14(裁决 G-a) 窗口选取用的是页几何端点,不是被裁定的那对链成员

- **现状**:`tools/splice_judge.py` 的 tail 取"该页阅读序最后一个端点候选"、head 取下一页
  第一个,这是 M1 的口径(合同 §2b.2)。对**展示标题切断**这类边界,被裁定切开的那对单元是
  两个 display 段落,而页尾/页首的几何端点是图注与标题——**14 行里有 6 行(两个 `2->3` 点
  的全部臂)因此没有测到被裁定的那条切断**,裁决按此把它们标为 `PROTOCOL-INVALID` 并排除
  出一致率分母。
- **这不是判官的问题,是材料的问题**:判官在那 6 行上对着送去的窗口给出的判词本身多数正确
  (如上游 `土著知识` 未译),只是那不是被裁定的拼接点。
- **补法**:`chain_signals.page_endpoints` 本就**逐类**给出端点("running text 的页尾与
  横贯该页的 display 行结束在不同地方"),而 M1 与 M10 现在都只取一个。窗口选取应按被裁定
  边界所属的那一类取端点(或两类都送判官),而不是取全页最后一个候选。
- **成本**:实现改动 + 重跑那 6 行(≤ 6 次判官请求)。
- **不补的措辞**:
  > 拼接点标注的窗口按页几何端点选取,因此只对正文流的切断成立;展示标题切断的行未能测到
  > 被裁定的单元,已从有效集中剔除并单列。
- **牵连**:M1 取 tail 的口径与此相同,故 `2->3` 这类边界上 M1 谈的也不是被裁定的那对单元。
  台账 A-16 的 `2->3` 判决须按此限定阅读。

### GAP-15(裁决 G-b) 判官不检查窗口的目标语言

- **现状**:`Courier-zh` 的两行 fork 臂窗口通篇中文(该跑实为 zh→zh,见台账 A-26),判官照常
  给出 `reading` 并报"无错误",**未指出目标语言不符**。这是 GAP-14 之外独立的一条判官失效。
- **补法**:两处可选,建议都做——(1) prompt 增加一条:窗口若不是目标语言,报
  `accuracy/untranslated` 并说明;(2) 工具侧按脚本统计做确定性预检,语言不符的行直接标注
  而不问判官。后者不花钱且不受判官抽样影响。
- **成本**:prompt 改动 + 受影响行重跑(≤ 4 次);确定性预检零 API。
- **不补的措辞**:
  > 判官只被要求判断拼接,未被要求核对目标语言;一份未翻译的产物在本协议下可能被读成
  > "拼接无错误",故语言状态须由产物侧另行断言。

### GAP-16(裁决 G-c) 判官把非拼接的词汇错误算成拼接错误

- **现状**:`Courier-en 7->8` 的 `chain_on` 行,判官记 `accuracy/mistranslation` **critical**
  并归因于拼接;人工裁定该行**没有拼接错误**(tail 收在真句末、head 另起一句),judge 抓到的
  `申请/已获得` 是**四臂皆有**的非拼接词汇错误,且严重度应为 minor。一次判词里同时出了
  **严重度偏高**与**归因错误**两个问题。
- **补法**:答案契约增加 `splice_attributable` 布尔字段(prompt 与校验同步),使"拼接归因的
  错误"与"窗口内顺带看到的错误"在数据结构上分开;人工裁决已按这个区分写(`human_note` 里
  的 non-splice 一律不计)。
- **成本**:prompt + 配置 + 全表重跑(14 次)。
- **不补的措辞**:
  > 判官报出的错误不区分"由拼接引起"与"恰好落在窗口内",故错误计数不可直接读作拼接缺陷
  > 计数;本文引用的拼接归因以人工裁决为准。

### GAP-17(裁决 G-d) 边界字符重复:`包` 在跨页处出现两次

- **现状**(本会话现场核实,证据在台账 A-26):`Courier-zh` 官方版的词中切断是
  `…协议中包` / `含惠益分享条款`,源文全篇只有**一个** `包`;fork 产物的页 7 尾仍是 `…中包`,
  而页 8 首段 `p8#8` 的译文字段是 `包含惠益分享条款。…`——引擎把被切断的残词 `含` **补全成**
  `包含`,于是 `包` 跨边界出现两次。判官读到 `包—包含` 却判为自然衔接、零错误。
- **机制**:该边界在本跑里 `not_chain_eligible`(`chain_report.json` 记 `tail,head` 皆不合格,
  因为 zh 侧页型全部塌成 `sidebar_heavy`,台账 B-06),链没有建立,残段因此按普通段落单独送译,
  引擎"修好"了它看到的残词。**这正是本项目要治的那类损害在链未生效时的表现形式。**
- **补法**:(1) zh 侧页型标定(链的准入条件依赖它);(2) 增设"边界字符重复"检测——源侧边界
  末字符在译侧于边界两侧各出现一次即报。后者是确定性的,零 API。
- **成本**:检测器实现小;zh 标定属语料侧决策。
- **不补的措辞**:
  > 当跨页链未建立时,被切断的残词会被逐段翻译"补全",在成品中形成跨边界的字符重复;
  > 该现象已观察到一例,尚无自动检测。

### GAP-18(裁决 G-e) 标题链的 proportional 重分配切在词中

- **现状**:`Courier-en 2->3` 的 `chain_on` 臂,页 3 首行排成 `动科学发现`。人工裁定:这是合并后的
  标题链被 `proportional` 策略按比例切开时**切在词内**(`推 / 动`)留下的残字,不是判官所记的
  "源文不支持的增字"。判官抓到的症状对、归因错。
- **机制**:`proportional` 策略用于没有句结构可切的展示单元(b5.1 起的设计),它按字数比例分配,
  不看词边界;M1 的 `designed` 档正是为这类成员设的。
- **补法**:proportional 分配点吸附到词/字类边界(中文按词法边界或标点,拉丁按空白),
  代价是分配比例的微小偏移。
- **成本**:实现小;须重跑受影响样张以更新产物。
- **不补的措辞**:
  > 展示单元的按比例重分配不保证落在词边界上,可能在成品中留下半个词;该行为由 `designed`
  > 档单列,不计入 mid-unit page-break rate 的分子。

### GAP-19(恢复批次 batch-b9.2r) 冻结产物被保留策略淘汰,四行降级为"引文存活、产物级复验已失"

- **成因**:batch-b9.2 会话一的门禁声明并创建了空的 `examples/output/b9_2/`,
  `configs/output_retention.json` 的 `keep_recent_batches: 2` 保护窗因此由 `[b9_1, b8_4]` 移到
  `[b9_2, b9_1]`,batch-b8.4 掉出窗口,其**未被 git 跟踪**的冒烟产物(页级栅格、各 stage 的
  checkpoint、部分 sidecar)在两次 sweep 收尾的 prune 中被删除。被跟踪的产物全部存活。
  逐路径清单在 `docs/eval/evidence_ledger.md` 顶部的"已失效产物登记"。
- **已排除的恢复路径(实测,不是推测)**:`examples/output/gate_cache/` 永不被 prune,内含全部
  六个样张的 stage-06 checkpoint;实测把它们拷回原位后,`docs/eval/results_e1/lopo_v2.json`
  的 fold matrix 重算结果**与被跟踪的那一份不一致**。gate_cache 的变体与 b8.4 那一跑不可互换,
  照此"恢复"等于静默篡改证据链,故拷贝已全部撤回。
- **为什么不重跑**:b8.4 之后 `babeldoc/magazine/` 的 role block 已改变 prompt 空间,同一配置
  重跑得到的是另一份产物。用新产物顶替原件,与上一条是同一种篡改。
- **降级(本批采用的处置)**:**A-26、E-02、E-06、E-13** 四行的状态由 `直接可引` 改为
  `artifact pruned, sha recorded`。语义是:引文内容与哈希存活于**被跟踪的**台账与批次报告,
  照台账文本引用不受影响;承载它的未跟踪产物已不在树内,**产物级复验已失**,且一律不重算。
- **根因修复(同批完成,防止再发)**:
  1. `configs/output_retention.json` 新增 `protected_paths`,并入 `spec_checks/spec_check_e0.py`
     登记的全部"路径 + sha256"与分级入库路径;prune 对它们一律不选。
  2. 被淘汰批次的 `*.report.md` / `*.json` / sidecar / `*.log` 自动打包进
     `docs/reports/archive/` 下的每批次 zip 并**入库**,大件仍删除。此后一次淘汰不再等于一次丢失。
  3. 门禁对 git 跟踪的 results_e1 / results_e2 产物(`docs/eval/`)一律**只读校验**:重算一律写入临时目录再逐字节
     比对,依赖缺失时报 SKIPPED 并指明缺失路径,绝不覆写盘上的冻结件。
     `spec_checks/run_all.py` 逐门禁前后取哈希,谁写了冻结件谁转红。
- **不补的措辞**(引用这四行时照此写):
  > 该数字的引文与哈希载于本仓库跟踪的证据台账与批次报告,可按其文本引用;承载它的中间产物
  > 已由输出保留策略淘汰,因此**不再可作产物级复验**。本文不以重跑替代原件:该批次之后
  > prompt 空间已变,重跑产出的是另一份产物,不构成同一观察的复现。

---

## 4. 机制批次开出的缺口(batch-b9.4 追加,证据来自 batch-b9.3 的三臂验收)

下列两条由 `examples/output/b9_3/report.md` 的 d 节与 f 节开出。两者都不是 b9.3 的收口对象,
但都限定了此后一切"页级/段级开关只改它声明的范围"与"裁决词条已落地"的主张能写多强。

### GAP-20 页级机制经文档级通道外溢到未声明页

- **现状**(batch-b9.3 三臂实测,`examples/output/b9_3/report.md` §d):行切分是**页级** pass,
  它的封闭性在**译前文档**上逐段成立——三份样张的差异页全部落在声明页内,重复臂(control)
  在任何页上都零差异。但在**成品译文**上不成立:未声明页也变,Courier-en 是页 2/4/7/8,
  FD-en-v2 是页 2/6。
- **两条通道**(逐页归因,不是推测):
  1. **全文共享的自动术语表**:`AutomaticTermExtractor` 读整篇写一份 glossary,每条翻译 prompt
     都引用它;声明页的文本一变,词条组成就变(Courier-en 变 7 条、FD-en-v2 18 条、
     Vogue-en 26 条),因此触及未参与切分的页(Courier-en 记到页 2/4/5/7/8)。
  2. **跨页/跨栏配对**:配对把两页的段落放进同一请求,请求组成随之改变(Courier-en 页 2,
     FD-en-v2 页 2/4/6/8)。
- **归因地板**:三份样张的 `unexplained` 均为 none——每一个变化的未声明页都能归到上面两条
  通道之一;Vogue-en 词条变了 26 条而**零页移动**,说明词条组成变化是必要条件而非充分条件。
- **补法**(任选,建议 a 或 c):
  - (a) **词表钉版**:A/B 两臂共用同一份冻结 glossary(取开关向下那一臂的产物)作为运行期
    输入,使文档级通道不随臂变化;跨页配对同理按声明页边界切开批次。
  - (b) 配对与批次组成在开关声明页处强制切分,代价是批次利用率下降。
  - (c) **只改口径**:逐字节封闭性主张一律限定在译前文档层面,成品层面按归因表呈报。
- **成本**:(a) 一处运行期输入钉版 + 受影响样张重跑(有 API 支出);(c) 零成本,纯呈报口径。
- **不补的措辞**:
  > 本项目的页级机制在译前的中间文档上只改动它所声明的页面,该封闭性逐段可验。成品译文中
  > 未声明页面的差异经两条文档级通道传出——全篇共享的自动术语表,与跨页配对的批次组成——
  > 二者在本文的对照实验中逐页归因,归因地板下无未解释项。故页级效应的逐字节封闭性只在
  > 译前文档层面主张,成品层面以归因表的形式呈报。

### GAP-21 裁决词条跨源行边界排布时不构成匹配面

- **现状**(`examples/output/b9_3/report.md` §f):14 对人工裁决的名称里,有 **2 对**在两臂都
  没有进入任何请求;其中 **1 对**(`CourierT H E UNESCO`)经核实的成因是**它在源版面上跨两条
  源行排布**,因此无论切分前后都不是一个连续字符串,匹配器无从命中。其余 12 对在切分前后
  的到达请求数逐对相同(唯一变化的 `The UNESCO Courier` 由 5 降为 4,是同一页被切成更小单元
  的直接后果,不是失配)。
- **为什么这是缺口而不是噪声**:术语匹配的作用面是**单个请求的文本**,而版面上的一个术语可以
  被排在两行上(标题字距拉开、竖排刊名、题图中断)。这类词条既不会报错也不会命中,在
  "裁决落地率"里静默地进入分母。
- **补法**:(1) 匹配面改为**行拼接文本**——对相邻源行做连接后匹配,命中后按行内偏移回写;
  (2) 或在段落级(未切分)文本上先做一遍匹配,再把命中区间映射到行段。两者都不改裁决文件。
- **成本**:实现小,零 API;需重跑受影响样张以更新落地率产物。
- **不补的措辞**:
  > 术语裁决的匹配面是单个翻译请求的文本。当一条词条在源版面上跨行排布时,它在切分前后都
  > 不是连续字符串,因此不进入任何请求,也不会被报为失败。本文报告的裁决落地率以能构成连续
  > 字符串的词条为分母,跨行排布的词条单列说明。

---

## 5. B9 线收官时仍开放的缺陷(batch-b9.5 追加,证据来自 batch-b9.5 的四臂验收)

下列五条是 B9 线五个批次(b9.1~b9.5)结束时**仍未收口**的已知缺陷,F2 报告的"已知局限"节
直接引用本节。证据一律出自 `examples/output/b9_5/report.md` 与 `examples/output/b9_5/evidence.json`
(全语料 6 样张 41 页,四臂:off / control / on / contain)。

### GAP-22 交并比对"小框压大框"的文-文重叠不敏感

- **现状**:全语料 66 对相交文段中,`text_text_collision` raised **0** 条(其中 **38** 对经源版面
  对照判为设计,**28** 对在 `collision_min_iou = 0.2` 之下)。但按"相交面积 / 较小框面积"重读同一批
  pair,有 **36** 对覆盖率 ≥ 0.5 而不被检出——其中 Vogue-en 页 3 的目录页码压条目
  (`p3#18`/`p3#19`,iou 0.1031,覆盖 0.5458)与断词压条目(`p3#35`/`p3#38`,iou 0.0269,
  覆盖 0.7178)在光栅上肉眼可见
  (`examples/output/b9_5/raster/Vogue-en.p3.census.png`)。
- **成因**:交并比的分母是两框并集。一个小框整体落在大框内时,交集≈小框面积,并集≈大框面积,
  比值趋近于面积比而非"压住了多少"。杂志版面里的页码、折标、断行残字正是这种形状。
- **补法**:在检测器里并列第二个度量(相交面积 / 较小框面积),两个度量各带自己的
  `allowed_range`,任一越界即报;源版面豁免同样按两个度量各自对照。本批次的普查表已经
  把这一列算出来,可直接作为定阈依据。
- **成本**:检测器一处改动 + `configs/detectors.json` 一个新有界参数;零 API,但需重跑全语料
  普查以更新证据。
- **不补的措辞**:
  > 本文的文-文碰撞检测以交并比为度量,并以源版面的同一度量作豁免。该度量对两个面积相近的
  > 文块敏感,对一个小文块整体落入大文块的情形不敏感;后者在本文语料中以目录页码与断行残字
  > 的形式出现,按"相交面积 / 较小框面积"可测而未纳入判定。故本文对"译文未产生文-文碰撞"的
  > 主张限定在交并比度量之下。

### GAP-23 `resolve_collision` 无自动动作,且本语料无诱发碰撞可供定型

- **现状**:动作已进词表并产出升级清单,v1 不动任何段落。本批次的普查给出了 b9.5 计划要求的
  分类(诱发 / 源设计 / 界下),结论是**本语料诱发碰撞为 0**:CERN 的拼版重影(同一串文本
  painted twice,iou 1.0)与 FD-en-v2 页 9 的刊头分层(iou 0.88~0.98)全部落在源设计一侧,
  Courier-zh 页 8(此前评审记录的"三层")在本次构建里根本不存在相交对。
- **为什么仍是缺口**:自动动作的成因分类需要至少一个诱发样本来定型,而语料里没有。这不是
  实现遗留,是证据遗留。
- **补法**:等 GAP-22 的第二度量落地后重跑普查(它会把目录页码类重叠纳入判定),再按新的
  诱发清单决定动作;或引入一份已知会诱发碰撞的样张。
- **成本**:随 GAP-22 一并支付。
- **不补的措辞**:
  > 文-文碰撞在本文中是报告项而非修复项。其成因在本文语料中全部归于源版面设计,自动修复
  > 所需的诱发样本不存在,因此本文不主张对该缺陷有修复能力。

### GAP-24 出页修复只受理显示类版面标签,7 项中 5 项被规则拒绝

- **现状**:全语料 `out_of_page` **7** 项。按来源分:**1** 项诱发(Courier-en `p1#10` 刊名,
  源版面完全在框内)、**1** 项源出血被译文加深(CERNCourier-en `p1#2` 报头,源 16.07pt →
  成品 36.69pt)、**5** 项源本已出血且译文未加深。按处置分:**2** 项被 `contain_in_page`
  受理并施加(即上面两项,均为 `title`),**5** 项被适用规则以
  `layout_label_outside_the_containment_classes` 拒绝并进入升级清单
  (AramcoWorld-en-v2 `p1#1`/`p1#2`/`p5#16`,FD-en-v2 `p2#8`/`p4#3`,全部为 `fallback_line`)。
- **为什么这是缺口**:被拒的 5 项在本语料上**全部**是源出血,且译文均未加深(印厂拼版条、
  图片版权行;`p5#16` 译文「希贾兹」左侧出页 307.35pt,源同段为 313.60pt,译文反而收回了
  6.25pt),因此逐条看这 5 次拒绝都得出了正确结果。缺口不在这 5 条上,而在**得出正确结果
  的理由**上:规则按版面标签整类排除,而不是按"源版面同一段落是否也出页"排除。本语料里
  这两条判据恰好同向——诱发的两项都带 `title` 标签,源出血的五项都带 `fallback_line`——
  所以标签判据从未在此被证伪。一份带有非显示类诱发出页的样张会直接把两者拆开,而语料里
  没有这样的样张。
- **补法**:把适用规则的条件从"标签属于显示类"改为"标签属于显示类 **且** 源版面同一段落在
  框内(或未被译文加深)",即用本批次已经算出来的 induced/bleed 分类作为独立的受理条件,
  使受理不再单靠标签成立;`fallback_line` 的正文段落仍不平移(平移会带走阅读顺序),但可
  升级为高优先级人工项。
- **成本**:动作适用规则一处改动 + 检测器需把源出页量写进 evidence;零 API,需重跑受影响样张。
- **不补的措辞**:
  > 页框容纳只施加于显示类版面标签的段落。其余出页项一律升级为人工项,其中源版面本身即出血者
  > 占多数;本文不主张对非显示类段落的出页有自动修复能力。

### GAP-25 修复环的动作选择由模型采样,长发现表下仍会漏掉页框容纳

- **b9.5 现状**:on 臂(模型自选)在 6 份样张上的选择是 `translate_orphan_lines` 或 `none`,
  **一次也没有选中 `contain_in_page`**,包括 Courier-en 与 CERNCourier-en 这两份唯一带有
  可受理出页项的样张(Courier-en 的 `p1#10` 满足全部适用条件,模型给出的理由却是"没有发现
  满足任何动作条件的项")。第四臂把决策改成脚本(点名全部出页项,由动作自身的规则筛),
  两处均被正确受理、施加、并通过守卫与守恒检查。
- **b9.6 后的现状**:两个落空决策点按 `cache_key` 逐位复现后作了两轮措辞迭代
  (`examples/output/b9_6/report.md`)。选中率由 **0/2 变为 1/2**:Courier-en 的 `p1#10`
  现被点名并选中 `contain_in_page`;另有一个"只有容纳合格"的合成决策点由不选变为选中。
  CERNCourier-en 的 `p1#2` 仍未被选中——该请求呈递 48 条发现,其中 16 条碎片簇、31 条残留,
  出页项是第 17 行;两轮的回复都把某条残留的 `layout_label` 说成 `fallback_line`(实际是
  `plain text` 与 `title`),即在长表下按主导类归纳而不逐条读字段。措辞层面到此为止。
- **为什么这仍是缺口**:机制正确不等于机制被触发。触发率仍是采样出来的,且失败模式已定位为
  发现表长度与类别不均衡,而不是措辞不清——余下的两条补法都在控制器,不在提示里。
- **补法**:(1) 决策请求按 kind 分轮,一轮只呈递一种 kind,使几何类发现不与残留类竞争;
  (2) 或对适用规则可判定的动作走确定性预选,模型只裁决其余。第 (3) 条(在提示里显式要求
  逐条比对 evidence 与条件表)已由 b9.6 执行完毕,买到 1/2,不再列为待补项。
- **成本**:控制器一处改动;每样张的决策请求数按轮数上升,有 API 支出。
- **不补的措辞**:
  > 修复环的动作选择由模型在每次迭代中作出,该请求按设计不走缓存。本文对页框容纳的验收在
  > 决策被脚本固定的臂上进行,报告的是动作被调用时的行为。默认路径上该动作是否被选中为采样
  > 结果:在两个逐位复现的真实决策点上实测为 1/2,在一个只有该动作合格的合成决策点上实测为
  > 选中;未选中的那一点的发现表长 48 条且由另一类别主导。不作触发率的能力主张。

### GAP-26 上游众数字号锚定未修,容纳按 em 框而非墨迹落位

- **现状**:F1 的报头越顶缺陷,b9.2.2 定因为 `typesetting.py:1350` 的众数字号锚定(72pt 报头
  与 7pt 署名同段,行距按小号锚定致越顶)。本批次按计划**不修上游**,在 magazine 层用
  `contain_in_page` 矫正结果:CERNCourier-en `p1#2` 下移 34.01pt、Courier-en `p1#10` 下移
  20.01pt,两处光栅前后对照均由被切掉字顶恢复为完整字面。
- **两条残留**:
  1. **成因仍在**:任何新样张的同形段落都会重新产生越顶,容纳每次都要再修一遍;修上游属排版
     算法变更,记为未来工作。
  2. **落位偏保守**:检测与容纳读的都是字符盒(em 框)而非可见墨迹,显示行的 em 框高于字面,
     因此容纳把段落下移的量大于肉眼所见的切除量。两侧同源,判定自洽,但落位比必要的更靠里。
- **补法**:(1) 上游把行距锚定改为按段内最大字号或按行分组(排版算法变更,需全语料回归);
  (2) 或在检测/容纳侧引入字体度量的可见上缘,使落位按墨迹而非 em 框。
- **成本**:(1) 高,触碰上游排版;(2) 中,需要字体 ascent 度量且要处理缺字回退。
- **不补的措辞**:
  > 报头越顶的成因在上游排版阶段的行距锚定,本文不修改该算法,而在排版之后、写出之前矫正其
  > 结果。矫正的几何依据是字符盒而非可见墨迹,故落位较必要值更靠版心内侧;本文报告的出页量
  > 与位移量均以字符盒为准,两侧同源。

**附:一条不列为缺口的观察。** Courier-zh 的标题排版 pass 在段落 `KeCnq` 上重排失败
(`KeyError: 'NotoSerif-Bold.ttf'`),已被该 pass 自身捕获并保留原状,不影响产出;记录于此以便
F2 引用时不将其误读为静默失败。
