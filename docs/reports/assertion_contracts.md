# 断言合同变更登记

门禁断言是合同:它写下一个命题,此后每次运行都在证明那个命题仍然成立。命题本身
被后续批次改掉时,处置只有三种——**收窄**(断更弱但仍为真的命题)、**扩写**(断更
强的命题)、**重指**(原命题已被推翻,改断推翻之后仍然为真的那个)。三者都不是删
除,删除会让"这条检查一直在跑"变成"这条检查一直在跑一句空话"。

本登记是这些变更的常驻处。它们此前记在 `WAIVERS.md`,那是不对的:豁免是"偏离约
定、附失效条件"的登记,而断言合同的变更是**永久性内容**,没有失效条件可写,放在
一份按失效条件组织的表里只会永远失效不掉。F3 收口时迁出,原编号保留——`b10.3`
的门禁注释与批次报告按 `W-B10-05` 的写法互相引用,改号会把这些引用打断。

每条记:**原命题 / 为什么它不再为真 / 现命题 / 净强度方向**。

---

## AC-01(原 W-B10-05)`spec_check_b9_2.py` 断言 04a:标题排版配置的范围声明

- **原命题**:`title_typeset.json` 的每个键,跳过 description、`on_floor` 与
  `_allowed_range` 之后,都必须声明自己的取值范围。
- **为什么不再为真**:b10.1 的 T2 引入两张 by_target 表,它们是**对象**,既非数字
  亦非词表,会落进"未声明范围"分支而误报。
- **现命题**:跳过列表改读 `title_typeset._STRUCTURAL_KEYS`(声明在模块里,日后新
  增结构键自动纳入);另加两条——`on_floor_by_target` 的每个值须在平键的词表内,
  `title_min_scale_by_target` 的每个值须在平键的 `_allowed_range` 内。同批次内断
  言 04b 另加两条拒绝用例(越界的按语言下限、词表外的按语言策略)。
- **净强度**:**扩写**。原先只有平键被绑住,现在两张表的每个值也被绑住。
- **批次**:B10.1。

## AC-02(原 W-B10-06)`spec_check_b9_5.py` 断言 01e 与 04f:碰撞动作

- **原命题**:`resolve_collision` **恒不写入**(01e 断该动作没有参数——有参数即
  "声明了它用不上的东西";04f 断一次点名碰撞的决策写不出任何东西)。B9.5 的理由
  是重叠成因异质、对策相反。
- **为什么不再为真**:b10.2 的 T2 定向推翻该决定,并给出机制与一个实施案例
  (AramcoWorld-en-v2 `p3#0`/`p3#4`,y 向 1.3875pt,coverage 0.5234 → 0.35,
  finding 消失而非仅减小)。B9.5 的推理未被推翻,被推翻的是它所立的两个条件。
- **现命题**:01e 改断该动作的 coverage 下限不低于检测器的同名下限——即这个检查
  一直声称在断的东西("动作严于检测器")。04f 的夹具是两块等大文本叠在一处,而这
  正是新规则**仍然拒绝**的类(面积不对称条件在两个等大的段里找不到"较小的那
  个"),故其实质断言(零应用、文档逐字节不变、拒绝理由进升级记录、停机原因不
  变)全部原样保留,只有理由常量从 `REASON_REPORT_ONLY` 改为 `REASON_AREA`,检查
  名随之从 `..._writes_nothing` 改为 `..._refuses_a_pair_of_equals`。
- **净强度**:**重指**。b9.5 门禁不再能证明"该动作不写入"——那个命题已经过期;它
  现在证明的是"该动作不动等大的一对",这是推翻之后仍然为真的命题。
- **后续条件**:若 `resolve_max_area_ratio` 被改动到 04f 的夹具不再落在拒绝侧,该
  夹具须随之重选,或该断言迁往拥有该条件的批次门禁。
- **批次**:B10.2。

## AC-03(原 W-B10-12)`spec_check_b10_3.py` 断言 01f:碎片缝合配置的结构键

- **原命题**:`configs/fragment_stitch.json` 的每个非词表键必须携带
  `_allowed_range`。
- **为什么不再为真**:b10.4 的 T3b 加入 `declared_page_switch`,它是一个运行属性名
  而非旋钮,没有取值范围可言。
- **现命题**:按 AC-01 的既有写法,跳过列表改读
  `fragment_stitch._STRUCTURAL_KEYS`,并追加一条——每个结构键的取值必须是合法标识
  符,即"确实是一个属性名"。
- **净强度**:**扩写**。原先该键根本不被检查,现在它的取值形状被绑住。
- **批次**:B10.4。

## AC-04(原 W-B10-13)`spec_check_b10_5.py` 断言 03f:栏级 reflow 的负向对照

- **原命题**(PLAN_B10_5_REV2 门禁第 2 条):CERNCourier-en p1 零位移,sidecar 记
  "excess 低于阈"而非施加。
- **为什么不再为真**:实测 p1 不是零位移页——其 editorial 版式的主栏 Σ|excess| 为
  47.5pt,两段共上移 18.0pt。计划为 CERN 所引的那组欠填数(10.4 / 54.0 / 71% /
  372.6,标为 p4)也只有最大值 54.0 能在 p4 复现,中位数与占比不能;该组数与本仓
  任何一页都不逐条对应。
- **现命题**(一般形式):每个未施加的栏必须带一个封闭词表内的理由且位移为零,且
  语料中必须确有"excess 低于阈"的栏(该轮 19 栏)。构造性的零位移面由 Vogue-en、
  FD-en-v2(零 flow 页)与 Courier-zh(目标语言在收窄之外)三样张承担。
- **净强度**:**重指**,且比原命题强——三个构造性零位移面强于任何一页的经验零。
- **后续条件**:一次带实测表的计划修订指定一个真正零位移的对照页,或本一般形式被
  写进后续计划。
- **批次**:B10.5。

## AC-05(原 W-B10-14)`spec_check_b10_5.py` 断言 04b/04c:位移的像素证明

- **原命题**(PLAN_B10_5_REV2 门禁第 3 条):位移段 ink 仅现新位置——按字面读,要
  求把位移前的段外框在关臂图上裁下、与位移后的外框在开臂图上裁下,两份裁剪逐字节
  相等。
- **为什么不再为真**:做不到,且不是实现问题。位移是点值而光栅是整格,位移非
  72/dpi 的整数倍时同一字形落在两行之间,被反走样进两行,两张图必然不同(实测 64
  个位移带全部不等)。
- **现命题**,三条可判定断言:(a) 非施加页两臂逐字节相同;(b) 施加页墨量守恒——
  灰度暗于 250 的像素数两臂相差 ≤1%(实测八页逐页精确相等);(c) 两臂在每页画出
  的矢量路径与图像放置完全相同,且每个词的文本与横坐标完全相同,唯一差别是部分文
  本的高度。
- **净强度**:**重指**,且 (c) 比像素比对更强——它排除"重绘成看起来一样",而像素
  比对做不到。
- **先例**:像素逐字节比对对亚像素位移不可用是普遍事实,本条为该类断言的写法先
  例。
- **批次**:B10.5。

## AC-06(F3 开)`spec_check_b7.py` 断言 02a 与 `spec_check_b7_2.py` 断言 05b:空列表的分节

- **原命题**:裁决文件的任何一节写成列表即拒收(`{"terms": []}` 应当被拒)。
- **为什么不再为真**:b10.4 让 `[]` 读作**"这一节我什么也没裁"**,理由写在
  `hitl._require_object` 的 docstring 里:裁决是人照着一份分节为列表的草稿手写的,
  空节自然写成 `[]`,为标点拒收一份文件是拒错了东西。带成员的列表**仍然拒收**——
  它的条目该当何解,没有任何地方定过。
- **现命题**:三个探针改带成员(`[{"a": "b"}]` / `[1]` / `["p"]`),仍断"非对象即
  拒";另**追加**一条正向:每一节写成 `[]` 时被读作空节且什么也没裁定。
- **净强度**:**扩写**。原先只断了一侧,现在这条边界的两侧各有一条断言,且"空列表
  合法"这件事第一次被钉住。
- **批次**:F3(成因在 B10.4)。

## AC-07(F3 开)`spec_check_b7.py` 断言 03a:导出的术语行报的是"观测"而非"定案"

- **原命题**:`hitl.export_terms` 的每行恰有四个字段
  `{source, auto_target, vote_count, first_page}`,其中 `auto_target` 是该词条的定
  案呈现。
- **为什么不再为真**:b10.4 把这两件事拆开。导出报的是**抽取器观测到的**呈现
  (`observed_target`);一行**落成什么**由声明的人名策略在上一层导出
  (`name_harvest.derive`),并在那里被断言(`spec_check_b10_4.py` 的 03e 逐行重新
  推导并比对)。
- **现命题**:行字段改为 `{source, observed_target, vote_count, first_page}`,值断
  言随之改指观测值 —— 即这一层真正拥有的那一半。
- **净强度**:**重指**。定案的断言没有消失,它搬到了定案发生的那一层,并且在那里
  更强(逐行重新推导,而不是比对一个常量)。
- **批次**:F3(成因在 B10.4)。

## AC-08(F3 开)`spec_check_b7.py` 断言 09a:裁决与草稿的关系

- **原命题**:committed 裁决的**每一条**都必须与草稿的机器建议不同——一条与草稿一
  致的裁决即为"机器把自己的草稿当裁决用"的证据。
- **为什么不再为真**:b10.4 之后,草稿对**人名形状**行的建议是**按声明的人名策略推
  导**出来的,而人也按同一条策略裁决。此时"一致"是策略在起作用,不是机器在自我采
  纳。实测:Courier-en 的裁决 14 条术语中 **6 条一致、8 条不同**,页型那一条不同。
- **现命题**:断"**不是整体复述**"——一份裁决只要在它携带的条目上**至少有一处**与
  草稿不同,就是人写的;**每一条都一致**才是那个失败(一个采纳自己草稿的机器会处处
  一致)。
- **净强度**:**收窄**,逐条判据降为整体判据。原命题所保护的失败模式**原样保
  留**;被放弃的是"一致即可疑"这一推论,而该推论在 b10.4 之后本就不成立。
- **代价**:一份只改动一条、其余照抄的裁决不再被这条断言抓住。抓它的是别处:
  `spec_check_b7_5.py` 的真值摘要钉住裁决文件的每一次变化,变更须随会话登记。
- **批次**:F3(成因在 B10.4)。

## AC-09(B11.1 开)`spec_check_b10_4.py` 断言 02b:默认档对 F2 台账的冻结

- **原命题**:**当前选中的**人名档,逐语言编译出的 system prompt,其 SHA-256 等于 F2
  台账逐行记下的那一个。断言以 `policy.person_names != FROZEN_POLICY` 开头,即"选中
  的档必须是 `transliterate`"。
- **为什么不再为真**:B11.1 把 `person_names` 由 `transliterate` 改为 `translate`
  (T3)。**四档文本与逐档 SHA 表逐字节未动**——门禁自己的另一条断言证明了这一点——
  变的只是选哪一档。
- **现命题**:比较对象改为 `transliterate` **这一档本身**(以
  `dataclasses.replace` 把加载到的策略对象的 `person_names` 固定为 `FROZEN_POLICY`),
  与 F2 台账逐语言比对。选中哪一档不再进入这条断言。
- **净强度**:**重指**。冻结从来是**文本**的属性而不是**选择**的属性,原断言把两件
  事写在了一起;拆开之后,文本的冻结照断,而"选中的必须是默认档"这一推论在一个已经
  建成四档矩阵的项目里本就不该是断言。
- **批次**:B11.1。

## AC-10(B11.1 开)`spec_check_b10_4.py` 断言 03e:裁决草稿的默认值按谁的档推导

- **原命题**:`reviews/*.review.json` 的每个人名形状行,其 `auto_target` 等于**当前
  选中的**人名档对该行 `observed_target` 的推导结果,且该行的 `policy` 字段等于当前
  选中的档。
- **为什么不再为真**:草稿导出一次即归用户所有(CLAUDE.md §5.12),机器会话只读。树
  上的四份草稿是在 `transliterate` 在位时导出的;T3 改档后,"行记录的档"与"树选中的
  档"必然不同,而草稿**不能**被重写。
- **现命题**:推导改用**该行自己记录的** `policy` 驱动,并断言这个值落在配置声明的
  档词表内。参数化的那一半——"默认值是该档对该行观测的函数,由调用同一个
  `name_harvest.derive` 现场重算"——原样保留。
- **净强度**:**重指**。被断的机制没有变弱:仍然是逐行重新推导并比对,只是驱动它的
  档由"树当前选的"改为"这份草稿当时用的",后者才是这份草稿真正该被问的问题。
- **批次**:B11.1。

## AC-11(B11.1 开)`spec_check_b10_4.py` 断言 04f / 05e / 06a:被淘汰产物的处置

- **原命题**:三条断言各自读 b10.4 的 working dir(排版 checkpoint、页型 checkpoint、
  chain 报告)与完整译后 PDF,缺失即 FAIL。
- **为什么不再为真**:`configs/output_retention.json` 的 `keep_recent_batches` 为 2。
  `examples/output/b11_1/` 一出现,b10.4 即落出保留窗口,其未入库的大件被淘汰(小件
  已按 §4.13 打包进 `docs/reports/archive/b10_4.zip` 并入库)。§4.13 同时规定,冻结
  产物一经淘汰**不得以重跑顶替**。
- **现命题**:按 b10.1 与 b10.3 已经建立的写法,把可淘汰产物与入库产物分开:入库产物
  缺失仍是 FAIL,可淘汰产物缺失报 **SKIPPED 并指名缺失路径**。
- **净强度**:**收窄**。这是保留策略的设计后果而不是本批的选择,且收窄的边界由
  `PRUNABLE_PRODUCTS` 显式列出——一份入库证据消失仍然是失败。
- **代价**:b10.4 的这三条断言此后不再实际执行。它们所证明的事实留在 b10.4 的批次
  报告与本登记里,不留在一条会一直报绿的检查里(所以报 SKIPPED 而不是 PASS)。
- **批次**:B11.1。

## AC-12(B11.2 开)`spec_check_b8_4.py` 断言 04e:保留策略由谁触发

- **原命题**:`spec_checks/run_all.py` 的驱动函数在跑完全部门禁之后**无条件**调用
  `prune_outputs()`,且该步骤带 `--apply` 而非空跑。
- **为什么不再为真**:B11.2 的 T4 模式一指出,这条断言把"策略被应用过"与"跑门禁就
  是应用策略的唯一途径"绑成了一件事,于是**跑一次门禁即为一次销毁动作**——为读
  b10.4 的证据而跑门禁的那次运行,同时取走了 b10.1 的证据。b10.1 / b10.3 / b10.4
  的八条断言先后转 SKIPPED,成因即在此(逐条清单见 b11.2 交付报告)。读一份记录不
  得是抹掉另一份记录的那个动作。
- **现命题**:三件事一起断——(1) 驱动函数仍在跑完门禁之后到达该步骤,该步骤仍带
  `--apply`,且它现在**接受一个请求参数**;(2) 该步骤在**未被请求时实际不发起任何
  子进程**、在被请求时发起(桩掉 `subprocess.run` 后正反各跑一次,不是读源码);
  (3) 驱动器**声明了**请求它的方式(`--prune-outputs`),且该方式**默认为关**。
- **净强度**:**重指**。原命题"策略被无条件应用"已被 T4 推翻;现命题断的是推翻之后
  仍然为真且更强的那个——策略仍然可达、仍然 --apply、仍然在门禁之后,而它何时发生
  由调用方声明。断言从"读源码里有这个字符串"改为"把这个函数正反各跑一次",是执行
  而非文本比对。
- **后续条件**:淘汰不再随门禁自动发生,回收磁盘须显式 `run_all --prune-outputs` 或
  `tools/prune_outputs.py --apply`。`examples/output/` 的增长因此改由人决定,这是本
  条用"证据不被门禁误伤"换来的代价,须在批次报告里逐次说明。
- **批次**:B11.2。

## AC-13(B11.2 开)`spec_check_b11_2.py` 断言 03b:检测计数的回归判据

- **原命题**(PLAN_B11_2_REV2 门禁第 3 条):实跑样张的 `issues.json` **各 kind 计数
  不高于** b10_5 on 臂基线。
- **为什么不再为真**:实测五个样张里有一处上升——CERNCourier-en 的
  `text_text_collision` 由 1 升至 2。逐段核对(`examples/output/b11_2/
  t2_regression_attribution.json`):新旧两条 finding 指向**同一对源段落**,两段的
  `box` 与 `unicode` 与基线**逐字段相同**,变的只有 `scale`(0.85 → 1.0)。成因是
  b11.1 的恒等短路复活:该段文本是一个文件名字符串,引擎原样退回,故不再被重组重排,
  保留了原本会被缩排搜索压到 0.85 的字号。碰撞检测器量的是**墨迹**而非盒,盒不变而
  字变大即多覆盖了旁边的标题,coverage 由 0.7035 升到 0.7427,第二段越过 0.4 的受理
  线。**没有段落移动、没有文本改变、没有新的重叠产生**;一对本来就被报一次的段落现
  在被报两次。这正是 T2 所测的暴露面落地。
- **现命题**:计数上升**只在有归因时**被接受,且归因必须是特定的一种——新 finding
  指名的每一个段落,其 `box` 与基线**逐字段相同**、其文本与基线**逐字符相同**。无
  归因行、归因数与升幅对不上、或任一段落的盒或文本变了,一律失败。
- **净强度**:**重指**。原命题"计数不上升"已被一次可归因的检测口径变化推翻;现命题
  断的是推翻之后仍然为真且**要求更多**的那个:上升必须附带正面证据,证明它是"同一张
  页被读得不同"而不是"页变了"。原命题只要一个数,现命题要一份逐段的盒与文本比对。
- **后续条件**:归因文件是本批产物,受 §4.16 的衍生件规矩管辖,已由本门禁的
  `GATE_EVIDENCE` 声明。若某一批的上升无法通过"盒与文本均未变"这一关,那就是真回归,
  该失败。
- **批次**:B11.2。

## AC-14(B11.2 开)`spec_check_b11_1.py` 断言 01d:恒等判据用哪一种文本比较

- **原命题**:恒等写回的分支由**文本**触达而非对象身份,且该文本比较是**归一化**的
  —— 首尾空白不算改变(`" F&D\n"` 对 `"F&D"` 判为恒等)、宽度变体是同一个标记
  (`"！"` 对 `"!"` 判为恒等)。
- **为什么不再为真**:b11.2 的 T5.2 定向推翻"哪一种比较"这一半(b11.1 裁决处置二)。
  归一化回答的是另一个问题——两串折叠掉宽度与合成差异之后是否长得一样——在这里回答
  它,就让一个把半角标点改写成全角形式的回复被判为"未改变"、源形被保留,**模型的标点
  决定被一个只为发现"模型原样退回输入"而写的比较悄悄推翻**。判据收紧为**逐字节相等**。
- **现命题**:分支仍由文本触达而非对象身份(**这一半原样保留**,含"死比较不得复现"
  与四条非输入回退用例);两条依赖归一化的用例**翻到另一侧**——首尾空白是改变、宽度
  变体是不同的标记。仅归一化相等的段落由写回方计数并逐条列出
  (`nfkc_equal_not_byte_equal` 与 `identity_criterion.report.json`),判定权归 b11.2 的
  门禁 06b。
- **净强度**:**重指**。b11.1 的门禁不再能证明"这个比较是归一化的"——那个命题已被推翻;
  它现在证明的是推翻之后仍然为真的那个:比较读的是文本而不是对象。判据本身的正负用例
  移交 b11.2 的 `check_06b`,那里连同"归一化只用于记录"一并断。
- **实测**:全语料只有**一条**落在两个判据的差里(FD:`请阅读中文版!` → `请阅读中文版！`),
  收紧后模型的全角形被写入;FD 第 5/6/8 页的 `F&D` **仍为单行**,b11.1 T1 修好的事没有
  被反转。
- **批次**:B11.2。

## AC-15(B11.5 开)`spec_check_b11_2.py` 断言 06d:这条断言证明的是哪一天的事

- **原命题**:`examples/output/b11_2/FD-en-v2/FD-en-v2.b11_2.pdf` 的第 5/6/8 页上,
  `F&D` 标签各自站在**一行**上(b11.1 T1 的成果)。
- **为什么不再为真**:命题本身仍然为真,而它**证明不了它被写来证明的那件事**。它读的是
  b11.2 冻结的那份 PDF,于是它说的是"b11.2 那天产出的文档如此",不是"今日之树仍然如此"。
  b11.3 的 `.*Mono` 修法把这个标签从公式改判为文本、从而使它被翻译,而这条断言在
  b11.3 与 b11.4 两批里**逐次通过**(GAP-36)。冻结件防的是证据被改写,防不了行为漂移。
- **现命题**:**最新一个写出 `render_evidence.json` 的批次**的 FD 产物,其第 5/6/8 页上
  `F&D` 各自站在一行上。判据(同左边距 0.5 pt 内、行距 8 pt 以上即算两行)与页码
  **一字未改**;改的只是"读哪一份产物"。
- **净强度**:**重指**,并且是收紧。原命题只能被一次历史事件证伪,现命题每批都要重新
  成立一次;b11.3 那次漂移在现命题下会当场为红。读取经 `spec_checks/evidence.py`,
  故工作区淘汰后自动回落归档。
- **为何取"最新"而非钉 `b11_5`**:钉死任何一批都会在该批出保留窗后退回同一个陷阱,只是
  换一份更新的冻结件。取最新把问题从"会不会再冻结"转成"有没有人继续写那个衍生件",
  后者在 `GATE_EVIDENCE` 与本条的 detail 行里都看得见。
- **实测**:b11.5 产物上为真(p3/p5/p6/p7/p8 均有 `F&D` 源形,无 `财政与发展`);
  该批 T1 的词表 keep 条目正是使它回到为真的那一步。
- **批次**:B11.5。

## AC-16(B11.5 开)`spec_check_b11_2.py` 断言 00b:对照项不可恢复,转 SKIPPED

- **原命题**:本批目录存在时,保留策略**不会**删除 b10.5 的受保护文件,**并且**它在
  b10.5 中**仍然选中其他文件**——后半句是对照项,没有它,一个"什么都不再选"的策略也会
  让这条断言通过。
- **为什么不再为真**:b11.4 的淘汰删掉了 b10.5 中一切可淘汰文件(GAP-37 第 2 条)。
  b10.5 现存的每个文件或受保护、或被 `keep_patterns` 留下,可选数恒为 0,且该目录**不会
  再获得新文件**。对照项**永久为空**。
- **现命题**:无。整条断言报 **SKIPPED 并写明成因**,而不是只跑前半句。
- **净强度**:**收窄至零**。这一条是本登记里少见的"不是重指也不是扩写"——它是承认一条
  断言已经不能再执行。之所以不保留前半句照常通过:一条只剩正向半边、却仍显示为绿的断言,
  正是 GAP-36 得以存活两个批次的那种形态;把它记成 SKIPPED,代价是少一条绿,收益是不再
  有人以为这里被覆盖着。
- **另一条可选补法(未采纳)**:GAP-37 的补法 (1) 建议把对照项**重指**为"策略在**任一**
  已出窗批次中仍有选择",那样它借 b11_5 的出窗可以自然成立。本批未采纳,因为
  `PLAN_B11_5_REV2` T1 明写"按 AC 先例转 SKIPPED-with-cause";重指的选项留在 GAP-37
  里,任何后续批次可以取用。
- **先例**:W-B11-07 的八条(GAP-31),同为证据不可恢复而维持 SKIPPED。
- **批次**:B11.5。

## AC-17(B11.5 开)`spec_check_b11_4.py` 断言 06b:裁决文件的钉钉的是什么

- **原命题**:`reviews/FD-en-v2.decisions.json` **整份文件**的 SHA-256 等于
  `examples/output/b11_4/t5_conservation.json` 里记下的那一个,并且它含
  `Masthead` 条目。
- **为什么不再为真**:整份文件的摘要**必然**在下一次裁决时失配。裁决是活文档,
  用户每加一行它就变一次;CLAUDE.md §4.12 正是为此写的——钉只锚定"机器零改动",
  用户更新时重钉。b11.5 的 T1 与 T4 加了四行(计划指派),这条断言随即为假,而它
  真正要说的那件事(`Masthead` 落到页面上)一点没变。另有一层:它比对的是
  **b11.4 自己的冻结件**,即 GAP-38 那个形态——冻结件说的是那天,不是今天。
- **现命题**:两句。(a) 文件仍含 `Masthead` 条目且其目标形与本批断言的一致;
  (b) 盘上文件的摘要等于 `spec_check_b7_5.TRUTH_DIGESTS` 的钉 —— 而那个钉**带
  逐次变更记录**(was/now/what/who/why),重钉是有据的动作而不是一次改数。
- **净强度**:**重指**。"有没有机器私自改过这份文件"这个问题一条没少,只是问在
  它该被问的地方:一处带变更记录的钉,而不是四处各自冻结的快照。b11.4 关心的那
  一行由 (a) 直接断,比原来的整份摘要更贴题——整份摘要在 `Masthead` 被删掉而别处
  补了同样字节数时也一样通过。
- **同批的另两处**:`b7_5.TRUTH_DIGESTS` 与 `b9_5.FD_RULING_DIGEST` **不是重指**,
  是 §4.12 指定的**重钉**,按 b9.1 / b9.2 / b11.2 三次先例原样办,不入本登记。
- **批次**:B11.5。

