# b11.9 —— 新增中文语料的基线与用户裁决表(无翻译跑)

前置 HEAD `3b163e9`。规格为 `plans/PLAN_B11_9.md`。单臂。**全批零 API**。

本批只做两件事:为缺基线的五份样张建基线,为六份新中文样张建立裁决表。
不修法、不改机制、不跑翻译。

---

## 一、前提校验(逐条,全部相符,无修正案)

| # | 前提 | 实测 | 出处 |
| --- | --- | --- | --- |
| 1 | registry 12 条,其中六条 zh→en 新样张 | 12 条;`HuaweiTech-zh` `ABB-zh` `bull-zh` `fd-zh` `ITU-zh` `WIPO-zh` 均 `zh->en` | `corpus/registry.user.json`(`owner` 声明在第 3 行) |
| 2 | manifest 前七条含 baseline,后五条只有 sha256 | 相符,缺基线者为 ABB / bull / fd / ITU / WIPO | `corpus/manifest.json` |
| 3 | page_labels 含十二样张,`HuaweiTech-zh` 六页 | 12 键;该样张 6 键,与文件实际页数(pymupdf 实测 6)一致 | `corpus/page_labels.json` |
| 4 | reviews 现存裁决表三份,六份新样张均无 | `Courier-en` / `Courier-zh` / `FD-en-v2`;六份新样张确无 | `reviews/` |
| 5 | 裁决表三段 + 元数据键;terms 为 object;page_kinds 页号→单值;drop_caps `pN#k`→verdict | 相符。`_require_object` 另接受 `[]` 作空段(`Courier-zh` 的 `"terms": []` 即此) | `babeldoc/magazine/hitl.py:266-308`、`hitl.py:146` |
| 6 | 合法 kind 共 15 个,`_validate_page_kinds` 拒表外值 | 15 个,名字与前提逐一相符;负向探针见 §六 | `configs/page_types.json`、`hitl.py:312-336` |
| 7 | `_validate_terms` 三条:非空、无首尾空白、规范化无碰撞 | 相符 | `hitl.py:286-308` |

计划零节引用的三处代码事实亦逐一核实:`only_parse_generate_pdf` 的提前返回在
`babeldoc/format/pdf/high_level.py:938-944`;`skip_translation` 只摘除
`ILTranslator` 阶段在 `high_level.py:303-304`;`hitl.after_page_classify` 在
`high_level.py:1017`。三处均与计划所述一致,故按计划原样执行。

---

## 二、T2 的零 API 判定(动手前先判,判定发请求即关闭)

计划与本次 prompt 都要求先判定术语抽取与人名收割是否发请求。逐条判定:

| 路径 | 是否发请求 | 依据 | 本批处置 |
| --- | --- | --- | --- |
| `AutomaticTermExtractor` | **是** | 需 LLM 引擎;`high_level.py:1040-1043` 在 `auto_extract_glossary` 为真且引擎支持 LLM 时调用 | `auto_extract_glossary=False`,阶段被 `high_level.py:301-302` 摘除 |
| 人名收割 render | **是** | `name_harvest.render_names` 每文档发一次批量请求,调用 `engine.llm_translate(...)`(`babeldoc/magazine/name_harvest.py:317-319`);经 `hitl.merged_terms`(`hitl.py:590`)触达 | **判定为发请求,故关闭**:`magazine_name_harvest=False` 显式置下 |
| `ILTranslator` | 是 | —— | `skip_translation=True`,阶段被 `high_level.py:303-304` 摘除 |

第四道:本跑 `translator=None`,故 `render_names` 的 `engine is None` 分支
(`name_harvest.py:294-300`)本就无请求可发。**四道彼此独立**,任一道单独成立即零
请求;四道同时成立是本批的实际配置。判定与配置写在驱动脚本
`docs/reports/b11_9/t2_draft_export.py` 的模块文档里,该脚本是本批 T2 的全部运行入口。

零 API 不以开关清单为证,而以**数据库行数**为证:每次跑前后各取一次
`_translationcache` 行数,六次跑的前后值恒为 **3136 → 3136**(逐样张见 §四)。
T1 的五次跑 `translator=None`,连缓存客户端都不存在。

---

## 三、T1 —— 五份样张的基线

`python tools/build_baseline.py --sample <file> --suffix .b11_9.pdf`,**逐样张单独调
用**:工具在全部样张跑完后才回写 manifest,分开跑使某份报错不至于连带丢掉其余已成
的基线。零 API,`only_parse_generate_pdf` + `_ParseOnlyDocLayoutModel`。

| 样张 | 页数 | 耗时 | 基线 PDF | sha256 | checkpoints.zip | api_calls | 报错 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ABB-zh | 9 | 17.0 s | `examples/output/baseline/ABB-zh.b11_9.pdf` (755 KB) | `6641e584b165dd1f6dcd9ea80d4051a55e9daade0e05defc31a9c2dba63eabc3` | 1.4 MB | 0 | 无 |
| bull-zh | 9 | 236.6 s | `examples/output/baseline/bull-zh.b11_9.pdf` (9.6 MB) | `de0818af0a8fc0ef714f33a8c09f8191192e667976864d8c79e2a6610647267c` | 125.6 MB | 0 | 无 |
| fd-zh | 6 | 18.8 s | `examples/output/baseline/fd-zh.b11_9.pdf` (846 KB) | `68b3c435b90b6b3217d3a9879cc00100cd1637669bb0e1714fdbd2cc5c10dfc9` | 2.9 MB | 0 | 无 |
| ITU-zh | 8 | 16.0 s | `examples/output/baseline/ITU-zh.b11_9.pdf` (3.4 MB) | `25e134f4f66eefec3c8ba4933341311a244736a2d892473c1f91029331f61100` | 1.2 MB | 0 | 无 |
| WIPO-zh | 6 | 16.8 s | `examples/output/baseline/WIPO-zh.b11_9.pdf` (583 KB) | `28446ef8aa870eb7…`(全值在 manifest) | 1.4 MB | 0 | 无 |

- **五份全部跑通**,无一份出现 GAP-50 那类 ToUnicode / CID 拒收,故无样张需按 GAP-50
  同款登记。六份样张的 `page.get_text()` 另行抽查,均返回正常中文码位。
- `HuaweiTech-zh` 已有基线(`.b2_2.pdf`),按计划不重建。
- 五个 sha256 已就地复核(重算文件哈希 == manifest 记录),五份皆 `True`。
- 全量 `run_all --set fast` 里的 `spec_check_b8_4.py ::
  check_04d_baselines_are_archives_that_round_trip` 覆盖 manifest 的全部基线并通过,
  即五份新归档能被读回。
- **bull-zh 的 checkpoint 归档 125.6 MB**,是其余四份的四十倍以上,耗时也高一个量级
  (236.6 s vs ~17 s)——该样张 pp6-7 是整版图示,IL 携带的图元数量远高于其他样张。
  这不是错误,但它使 bull-zh 的 checkpoint 单份就超出 `archive_max_file_kb`
  (2048 KB)两个数量级,即**它永远进不了归档**(CLAUDE.md §16 所指的那类)。故本批
  的验收证据一律取自小体积衍生件 `docs/reports/b11_9/b11_9_evidence.json`(4.9 KB),
  不读 checkpoint。

manifest 的改动仅为五个 `baseline` 块的新增(30 insertions / 5 deletions,`notes` 行
的删改只是行尾逗号),无其他字段变动。

---

## 四、T2 —— 六份零 API 草案导出

`python docs/reports/b11_9/t2_draft_export.py`。开关:`skip_translation`、
`magazine_page_classify`、`magazine_chain_detect`、`magazine_article_group`、
`magazine_hitl_export`;属性开关 `magazine_drop_cap_mark=True`、
`magazine_name_harvest=False`、`magazine_repair=False`;`magazine_hitl_apply=False`
(本批只导出,不应用任何裁决)。版面模型用真实 ONNX 模型而非 parse-only:页型与首字
候选都要读版面标签。

| 样张 | 耗时 | cache rows 前→后 | api_calls | page_kinds 行 | terms 行 | drop_caps 候选 |
| --- | --- | --- | --- | --- | --- | --- |
| HuaweiTech-zh | 84.3 s | 3136 → 3136 | **0** | 6 | 0 | 1 |
| ABB-zh | 76.1 s | 3136 → 3136 | **0** | 9 | 0 | 0 |
| bull-zh | 204.7 s | 3136 → 3136 | **0** | 9 | 0 | 0 |
| fd-zh | 95.3 s | 3136 → 3136 | **0** | 6 | 0 | 0 |
| ITU-zh | 77.0 s | 3136 → 3136 | **0** | 8 | 0 | 0 |
| WIPO-zh | 63.8 s | 3136 → 3136 | **0** | 6 | 0 | 0 |

`terms` 行全为 0:抽取器与人名收割都已关闭,这是计划预期的形态,不是缺陷。

**候选计数的两条说明(均为观察,本批不修)**:

1. 六份共 **2** 条下沉首字候选(HuaweiTech-zh `p4#11`「目」ratio 2.647;
   WIPO-zh `p2#1`「格」ratio 6.221),两条经原文核对**都是真候选**。
2. registry 的 notes 另记了两处下沉首字(bull-zh p3 总干事前言、ITU-zh p7),标记 pass
   一条也没找到;WIPO-zh p6 开头的「没」(同样是整段放大首字)也未受理。中文源上的
   **召回明显偏低**,已登记为 GAP-54。

---

## 五、T4 —— 六份裁决表

`reviews/<sample>.decisions.json`,六份。元数据按现存三份的写法 conform
(`format_version: 2`、`sample`)。

### 5.1 page_kinds:全页覆盖,每页单值

44 页全覆盖(6+9+9+6+8+6)。值一律取自 `corpus/page_labels.json` 该页的真值数组,
故**裁决表与真值的不一致清单为空**——每一个裁决值都是该页真值数组的成员。真值文件
未动。

真值为多标签的 11 页须择一,判据是**该页的下游 policy**(`configs/page_types.json`
的 `policy` 段),不是数组次序。逐页理由:

| 样张 | 页 | 真值 | 裁决 | 理由 |
| --- | --- | --- | --- | --- |
| HuaweiTech-zh | 1 | masthead, article_opener | **article_opener** | 版面为版权/编委栏 + 刊首语正文。`masthead` 声明 `preserve_line_structure`,会把刊首语那段散文按源行切成记录;正文体量(实测 `text_coverage_ratio` 0.371、`mean_paragraph_chars` 79.2)远高于名录栏,取正文语义 |
| HuaweiTech-zh | 4 | article_opener, article_body | **article_opener** | 该页起「电信运营商AItoB发展策略演进研究」:大标题 + 作者 + 导语 + 下沉首字「目」。两者的 `chain_eligible` / `indent_eligible` 相同,差别在 `opens_article` / `starts_article`,而该页确为开篇 |
| HuaweiTech-zh | 6 | article_opener, article_body | **article_opener** | 该页起另一篇(「超越连接:电信运营商的AI变现之路」,Brian Washburn),`starts_article` 为真,正好拦住 p5→p6 的伪链 |
| ABB-zh | 9 | article_opener, infographic | **article_opener** | 该页是自成一篇的 800xA Simulator What-if 简报(registry 明记 p8→p9 **不是**链)。实测 `image_area_ratio` 为 **0.000** —— 版面里的流程图是矢量/文字标注,`infographic` 缺乏版面证据;正文为两栏散文。`article_opener` 的 `starts_article` 同样拦住 p8→p9 |
| bull-zh | 6 | infographic, article_opener | **article_opener** | 见下方专条 |
| bull-zh | 7 | infographic, article_body | **article_body** | 见下方专条 |
| fd-zh | 2 | toc, masthead | **toc** | 两者 policy **完全相同**(`chain_eligible:false` + `preserve_line_structure` + `grid`),故择一不改变任何下游行为;版面主体是目次网格,版权栏是侧栏 |
| fd-zh | 3 | toc, editorial | **editorial** | 此处 policy 有实质差别:`toc` 会 `preserve_line_structure`,而该页主体是「编者的话」整栏散文(实测 `text_coverage_ratio` 0.621,全批最高;`mean_paragraph_chars` 51.7;`leader_dot_line_ratio` 为 0,即无目次引点线)。把散文按记录设行是这页唯一真实的损失 |
| fd-zh | 5 | article_body, infographic | **article_body** | registry 明记 p4→p5 是**真实的句中切断**(「疲弱」→「的生产力增长将严重威胁到…」)。`infographic` 的 `chain_eligible` 为 false,会使该切断不可接合 |
| ITU-zh | 3 | toc, masthead | **toc** | 与 fd-zh p2 同理(两者 policy 相同);另有刊物自证:该页页脚印的就是「目录」 |
| ITU-zh | 8 | article_body, sidebar_heavy | **article_body** | registry 明记 p7→p8 是同一篇的干净边界。`sidebar_heavy` 的 `chain_eligible` 为 false,会断掉该链;页面主栏确为上页正文的续 |

**bull-zh p6/p7 专条(本批唯一一处需要权衡的择一)**。这两页确实图示主导:实测
`text_coverage_ratio` 0.126 / 0.106,`image_area_ratio` 0.469,`mean_paragraph_chars`
13.7 / 15.0 —— 单看版面,`infographic` 是更贴切的**描述**。取正文语义的理由是另一侧
的代价不可逆:registry 记 p6→p7 为**真实的句中切断**(「…从实物保护到」→
「核安保文化。」),而页边界的判定处
(`babeldoc/magazine/chain_signals.py:1346-1355`)要求**两端都** `chain_eligible`,
`infographic` 声明的是 `false` —— 任一页判为 infographic,这条切断就永远接不上,译文
里那句话保持断开。反向代价是 `repair_profile` 由 `figure` 变为 `poster` / `flow`,
以及 `indent_eligible` 抬起;后者只作用于 running body text 标签的段落
(`babeldoc/magazine/indent_policy.py` 开篇所述四条件之一),图示的标注文字不在其列。
即:一侧是语义断裂,一侧是版面修复档位偏差,取前者为重。

### 5.2 drop_caps:候选 2 条,偏离 0 条,六份该段均为空对象

- 引用一律抄自 T2 草案,未手写页段号。
- `configs/drop_cap.json` 的 `default_decision_by_target` 对 `zh` 与 `en` 都声明
  `keep`(b11.8 起),而两条候选经原文核对都应 `keep`:两处源版面都把首字设计成放大
  首字,译为英文后按拉丁排法立起来是对的。**故两条都随默认,零偏离**。
- 按计划,只对需要偏离默认的候选出裁定,因此六份的 `drop_caps` 都是 `{}`。
  **空段与漏写不同**:候选 2 条、偏离 0 条,此处显式记录。
- 空段使 T5.1 的引用检查在本批没有正例可查,故另跑负向探针证明该检查不是空转
  (见 §六)。

### 5.3 terms:自源文本人工撰写,只钉三类例外

六份均为 zh→en,`translate` 档下纯中文人名的拼音输出即正确,故**一条中文人名也没
钉**(许志远、马海旭、王永德、刘鑫、赵东…… 全部不钉)。所钉者:

| 样张 | 条数 | 构成 |
| --- | --- | --- |
| HuaweiTech-zh | 9 | 类 c:华为 / 华为技术有限公司 / 中国信息通信研究院 / 中国信通院(CAICT) / 中国电信 / 中国移动 / 中国联通 / 中国通信学会;刊名保持原样:`HuaweiTech` → `HuaweiTech`(以 `target == source` 表达) |
| ABB-zh | 1 | 类 c:`ABB 评论` → `ABB Review`(刊物自有英文名)。该样张无中文转写的外文人名:署名(Björn Rosengren、Ruomu Tan)与产品名(Dynafin™、ProKiln、VD4 evo、800xA)在源文本里已是拉丁字母,无拼音化风险,故不钉 |
| bull-zh | 11 | 类 b:拉斐尔·马里亚诺·格罗西 → Rafael Mariano Grossi;阿布杜斯·萨拉姆国际理论物理中心。类 c:国际原子能机构 / 原子能机构(IAEA) / 国际原子能机构通报 / 核安保丛书 / 不扩散核武器条约 / 核材料实物保护公约 / 核安保综合支助计划(INSSP,全刊高频且是官方计划名) / 中国国家核安保技术中心 / 维也纳国际中心 |
| fd-zh | 46 | 本批的类 b 主场:35 条中文转写的西文人名(尼古拉斯·克拉夫茨 → Nicholas Crafts、罗曼·杜瓦尔 → Romain Duval、塞利娜·罗雄 → Céline Rochon…),拼音化会产出不存在的名字;另 11 条类 a/c(金融与发展、国际货币基金组织、四所被转写的大学、佩恩表 → Penn World Table、雷曼兄弟) |
| ITU-zh | 11 | 类 b:马里奥·马尼维奇 → Mario Maniewicz(两种分隔符各一条,加单姓一条)、安东尼奥·古特雷斯、阿卜杜勒·法塔赫·塞西。类 c:国际电联(ITU) / 国际电联新闻杂志 / 国际电联无线电通信局 / 世界无线电通信大会 / 无线电规则 / 埃及通信和信息技术部 |
| WIPO-zh | 10 | 类 b:泰勒•斯威夫特 → Taylor Swift、蕾哈娜 → Rihanna。类 c:世界知识产权组织 / 产权组织(WIPO) / WIPO杂志 / 东加勒比音乐版权集体组织 / 知识产权青年大使 / 维珍唱片 / 布鲁内尔大学。术语一致性 1 条:索卡 → soca(全篇 7 处,拼音化会产出 "Suoka") |

**撰写后过了两道机械校验**:

1. **来源核对**:每一条 `source` 都在该样张源文本里实际出现(逐条计数)。原写的
   `泰勒·斯威夫特`(分隔符 U+00B7)在 WIPO-zh 中**零命中** —— 该刊两处都用 U+2022,
   故该条已删,只留 `泰勒•斯威夫特`。另有 7 条只在"去空白后"命中(如
   `不扩散核武器条约`),那是 PDF 抽取在名字中间断行所致,IL 段落文本是连续的,保留
   其规范形式。**分隔符按刊物实际用字钉**:ITU 的马尼维奇在 p2/p3 用 U+00B7、p7 用
   U+2022,两种都钉;fd-zh 的克拉夫茨在 p2 用 U+00B7、p3 用「U+2022 + 空格」,两种
   都钉。
2. **`_validate_terms` 三条**:六份全过(非空、无首尾空白、规范化无碰撞)。长短同源
   的两对(`国际原子能机构通报` vs `国际原子能机构`、`世界知识产权组织` vs
   `产权组织`)规范化后不碰撞,且 `Glossary._build_regex_and_lookup` 按长度降序优先
   长匹配,故长名先中。

**明确未钉的三类(理由写在这里,而不是靠沉默)**:

- **中文转写的地名**:塞伯斯多夫(Seibersdorf)、的里雅斯特(Trieste)、沙姆沙伊赫
  (Sharm el-Sheikh)、达喀尔(Dakar)。失败模式与类 b 相同(拼音化产出不存在的地名),
  但计划把三类例外明确枚举为**人名 / 机构 / 品牌 / 刊名**,地名不在其中。在一份归用户
  所有的裁决文件上自行加宽类目,比漏钉几条更糟,故按计划的边界停手并在此列名备查。
- **官方英文名与拼音顺序不同的中文人名**:张涛(F&D 英文版印 "Tao Zhang")、赵厚麟
  (ITU 印 "Houlin Zhao")。两人的英文署名都印在**同刊的英文版**而不在本节选里,故不
  落在类 a 的字面(「刊物自己印出的英文署名」)。按计划「纯中文人名不逐条钉」处理。
- **一次性行业词**:舞厅乐(dancehall)、雷鬼乐(reggae)、摆线螺旋桨、具身智能等。
  按「宁少勿滥」不钉;其中舞厅乐若直译为 "ballroom music" 是实打实的误译,列名备查。

### 5.4 机器判定 vs 人工裁决(草案仅作对照,非真值)

两个数字,口径不同,不要混读:

- **对真值数组**(命中数组任一元素即计一致,`spec_check_b3_3` 的口径,全量 sweep 实
  测):十二样张合计 **41/85 = 0.4824**;六份中文新样张分别为 abb_review 2/9、
  iaea_bulletin 1/9、itu_news 3/8、wipo_magazine 1/6、huawei_tech 3/6,
  imf_fd 8/15(含 en 侧 FD-en-v2)。
- **对本批的单值裁决**(更严,因为多标签页只留一个正确答案):六份合计
  **9/44 = 0.205**。

两个口径都指向同一件事:确定性分类器在中文刊上的表现远低于英文刊
(unesco_courier 10/16、aramcoworld 7/9、vogue_us 3/3),且六份的 44 页里有 35 页
自报 `ambiguous: true`。这不是本批要修的东西,但它是一条**新的、成规模的**观察,已
登记为 GAP-55。

### 5.5 先前起草稿的处置

GAP-51 记的两份起草稿(`reviews/Vogue-zh.decisions.json`、
`reviews/HuaweiTech-zh.decisions.json`)格式不可加载(terms 写成 list、另立了 `keep`
数组、drop_caps 写成 `[{"page":3,"char":"目",...}]`),两份已不在树上。本批**未复用
其格式**:`HuaweiTech-zh` 按 §5.1–5.3 的规则重写,起草稿仅作内容素材 —— 它记的
「p3 目」经本批标记 pass 落在文件 `p4#11`,与 GAP-52 记的一页位移一致。
`Vogue-zh` 不在语料内(GAP-50),本批不涉。

因 `reviews/HuaweiTech-zh.decisions.json` 这个路径**又回到了树上**,
`docs/eval/gap_register.md` 中 GAP-51 末的失效引用块已相应更新:一条声称"已不在树上"
而实际存在的登记是假的登记。

---

## 六、T5 —— 验收(用既有装置,本批无新门禁)

| # | 验收项 | 结果 |
| --- | --- | --- |
| 1 | 六份裁决表逐份经 `hitl.parse_decisions` 校验 | **6/6 通过**(见下);既有三份复跑不回归 |
| 2 | T1 + T2 全部运行的 `api_calls` 合计 | **0**(11 次运行;缓存行数 3136 全程不变) |
| 3 | manifest 完整性 | **12/12** 样张全部有 baseline,报错者 0 |
| 4 | 不越权 | `corpus/registry.user.json`、`corpus/page_labels.json`、`corpus/chain_labels.user.json` 的 `git diff` **为空** |
| 5 | `run_all --set fast` | **28/28 gates passed in 3110.2s**,无 EXPECTED-RED |

**T5.1 逐份结果**(`pages` 取自 manifest,`references` 取自该样张 T2 草案的候选集):

```
OK   HuaweiTech-zh: terms=9  page_kinds=6 drop_caps=0 candidates_in_draft=1 pages=6
OK   ABB-zh:        terms=1  page_kinds=9 drop_caps=0 candidates_in_draft=0 pages=9
OK   bull-zh:       terms=11 page_kinds=9 drop_caps=0 candidates_in_draft=0 pages=9
OK   fd-zh:         terms=46 page_kinds=6 drop_caps=0 candidates_in_draft=0 pages=6
OK   ITU-zh:        terms=11 page_kinds=8 drop_caps=0 candidates_in_draft=0 pages=8
OK   WIPO-zh:       terms=10 page_kinds=6 drop_caps=0 candidates_in_draft=1 pages=6
OK   Courier-en / Courier-zh / FD-en-v2(既有三份,复跑不回归)
```

**负向探针**(因本批 `drop_caps` 全为空段,须证明该检查不是空转):

```
OK   negative reference: drop_caps['p99#1']: no such paragraph in this document
OK   negative kind:      page_kinds['1']: 'not_a_kind' is not a declared page type
```

逐样张的验收数据另存为衍生件 `docs/reports/b11_9/b11_9_evidence.json`(4.9 KB,
落在 `archive_patterns` 与 `archive_max_file_kb` 之内)。该文件**不被 git 跟踪**:
`.gitignore:19` 对 `*.json` 全局忽略,只对 `configs/` `corpus/` `reviews/`
`docs/eval/**` 四处开例外(`.gitignore:37-40`),`docs/reports/` 不在其中。本批不改
`.gitignore`(不在计划的改动范围内),而是把它承载的每一个数都写进本报告的表里 ——
本批不新增门禁,故无门禁读它;它在工作区与归档里作机器可读的对照件存在。

---

## 七、改动清单

新增:

- `reviews/{HuaweiTech-zh,ABB-zh,bull-zh,fd-zh,ITU-zh,WIPO-zh}.decisions.json`(裁决表,6)
- `reviews/{同上}.review.{json,html}`(机器草案,12)
- `examples/output/baseline/{ABB,bull,fd,ITU,WIPO}-zh.b11_9.pdf` 及对应
  `*.checkpoints.zip`(冻结件,10)
- `examples/output/b11_9/`(T2 运行产物)
- `docs/reports/b11_9/{b11_9.report.md,b11_9_evidence.json,t2_draft_export.py}`
- `plans/PLAN_B11_9.md`

改:

- `corpus/manifest.json`(仅五个 `baseline` 块)
- `docs/eval/gap_register.md`(新增 GAP-54、GAP-55;GAP-51 末的失效引用块更新)

未触碰(负向范围,以 `git diff` 为证):`corpus/registry.user.json`、
`corpus/page_labels.json`、`corpus/chain_labels.user.json`、`babeldoc/` 下全部文件、
`configs/`、`prompts/`、`spec_checks/`。**本批未新增门禁** —— T5.1 由既有装置
(`hitl.parse_decisions`)承担,故计划里"限该条"的例外未被动用。

`UPSTREAM_DIFF.md` 无增量:本批未改动任何上游文件。
`WAIVERS.md` 无增量:本批未产生偏离。

---

## 八、遗留

1. **GAP-54**:中文源上的下沉首字召回偏低(registry 记 4 处,标记 pass 受理 2 处)。
2. **GAP-55**:确定性页型分类器在六份中文样张上接近随机(逐刊数字见 §5.4)。
3. **六份裁决表现为机器起草**,按 CLAUDE.md §5.12 **自此归语料所有者所有**,机器会话
   只读。所有者复核时最该看的三处:fd-zh 的 35 条人名转写(最易钉错)、bull-zh p6/p7
   的择一(§5.1 专条)、以及 §5.3 末列出的三类未钉项。
4. **GAP-53 未动**:五份新样张的历史验收维度补齐仍待专开语料验收批。
5. **W-B11-23** 仍挂性能批,本批未跑该批相关的任何东西。
