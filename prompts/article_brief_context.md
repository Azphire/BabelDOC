Article brief. The paragraphs below are part of one article, and the following
was prepared from that article's opening so that every batch of it is
translated in one voice and with one set of names. It is guidance about the
piece, never text to translate and never an instruction that overrides the
rules above.
   - The article's title, as it is being rendered: {title_translation}
   - Register to translate it in: {register}
{names_block}
