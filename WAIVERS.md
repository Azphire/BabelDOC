# Waivers Registry

Deviations from the conventions in CLAUDE.md. Each waiver states the condition
under which it expires.

| id | scope | reason | expiry-condition | batch |
| --- | --- | --- | --- | --- |
| W-B0-01 | `spec_check_b0.py` negative assertion 8 | PLAN_B0 assertion 8 limits modified upstream files to three Python files, but T0.3 of the same plan mandates `.gitignore` entries. The gate enforces the three-file set for Python sources and a separate one-entry allow-list for `.gitignore`. | A future plan revision states the `.gitignore` change explicitly in assertion 8. | B0 |
| W-B1-01 | `il_version_1.rng`, `il_version_1.xsd`, `il_version_1.py` hand-edited | `docs/CONTRIBUTING.md` documents the generation chain (`trang` RNC -> RNG/XSD, then `xsdata generate ... --package babeldoc.format.pdf.document_il`), but it is not reproducible here: `trang` is not installed, and the installed `xsdata` 26.2 regenerates the whole module in a different style (`kw_only`, no `required` metadata, postponed annotations, required fields without defaults) even from the unmodified XSD, so regeneration would rewrite the file wholesale instead of adding fields. The three files were therefore synchronised by hand in the existing style, and `spec_check_b1.py` assertion 01c compares the attribute tables of all three schemas to catch drift. | `trang` and the `xsdata` version that produced the committed module are both available and regenerating from the unmodified XSD reproduces `il_version_1.py` byte for byte. | B1 |
| W-B1-02 | `spec_check_b1.py` negative assertion 9 | PLAN_B1 assertion 9 requires the `git diff` upstream set to be a subset of the IL schema quadruple, but the B0 upstream changes are still uncommitted in the working tree and therefore always appear in that diff. The gate asserts instead that the batch delta (changed files minus the B0 allow-list and `.gitignore`) is exactly the quadruple, and that every changed upstream file is registered in `UPSTREAM_DIFF.md`. | B0 and B1 are committed, after which the literal subset check can be restored. | B1 |
| ~~W-B1-02~~ LIFTED in B2 | n/a | Expiry condition met: B0 and B1 are committed as `batch-b0` and `batch-b1`. Assertion 09b now asserts the plain subset relation over the working tree delta against HEAD, with no presence conjunct, and both historical gates filter that delta down to upstream-owned paths so later batches do not trip them. | n/a | B2 |
| W-B3-01 | `spec_check_b2.py` assertion 10 | The assertion read "the magazine package contains no network or LLM client", a proposition B3 makes false by design: T3.2 adds the project's first model call point. PLAN_B3 does not say what becomes of it, so the proposition was narrowed rather than deleted: network egress exists in the declared modules of `MODEL_CLIENT_MODULES` and nowhere else in the package, which is the property the original assertion was protecting. Authorised by the corpus owner after the fact. | None; the narrowing is permanent. A later batch adding a second call point declares that module in `MODEL_CLIENT_MODULES` rather than widening the scan. | B3 |
| W-B0-02 | XML checkpoint fidelity | `il_creater_active.py` writes `debug_info="a"` into a field typed `bool \| None`, so xsdata emits a `ConverterWarning` when a checkpoint is read back. The value survives the round trip; only the warning is new. Boolean attributes also normalise from `0`/`1` to `false`/`true` on the first re-serialisation, so a written checkpoint is not byte-identical to its reserialisation (it is idempotent from the second write onward). | The upstream IL producer emits a valid boolean, or the schema widens the field. | B0 |
| W-B7-02 | `magazine/drop_cap.py`, `magazine_drop_cap_mark` | Every magazine switch so far is a `TranslationConfig.__init__` parameter, which is an upstream edit. T7.2 is delivered under a zero-upstream-change constraint, so this one is read off the configuration object with `getattr(..., False)` and set by whoever builds that object; `spec_checks/artifacts.py` carries it in an `attributes` entry of the mode rather than in the constructor call. Consequences: the switch is invisible to `inspect.signature(TranslationConfig.__init__)`, so a gate cannot assert its default from the signature as it does for the others, and a caller misspelling it gets a run with marking off rather than a `TypeError`. The dependency it does have -- `magazine_article_group` -- is checked explicitly and refuses the run. | A batch is authorised to touch `translation_config.py` again, after which the flag becomes a constructor parameter like its siblings and the `attributes` mechanism can go. | B7 |
| W-B7-01 | `magazine/hitl.py`, `apply_terms` | PLAN_B7's T7.1 injection assumes a ruling placed in `user_glossaries` reaches the translator. It does not when automatic extraction is enabled: `TranslationConfig.get_glossaries_for_translation` (translation_config.py:130-140) returns `[auto_extracted_glossary]` alone whenever that slot is filled and `auto_extract_glossary` is true, discarding every user glossary. The corpus owner chose the injection-side rebuild over an upstream change to that rule, so the injection additionally moves the rebuilt automatic glossary into the user glossary list and empties the automatic slot: both tables then reach the translator under either setting, and no upstream behaviour is edited. Consequence: with `magazine_hitl_apply` and `save_auto_extracted_glossary` both on, `pdf_creater.py` and `result_merger.py` read an empty slot and write no `auto_extracted_glossary` CSV beside the output. The same content, plus what the ruling dropped from it, is recorded in `hitl_apply.report.json`. | The selection rule returns user glossaries alongside the automatic one, after which the ruling can stay in the user list and the automatic slot is left as it was. | B7 |
| W-B8-01 | `magazine/react/`, `magazine_repair` | T8.2 is delivered under a zero-upstream-change constraint, so the repair switch is not a `TranslationConfig.__init__` parameter like its siblings: it is read off the configuration object with `getattr(..., False)` and set by whoever builds that object, exactly as `magazine_drop_cap_mark` is under W-B7-02, and `spec_checks/artifacts.py` carries it in an `attributes` entry of the `repaired` mode. The loop is reached through the `magazine_detect` hook the previous session already registered rather than through a call site of its own: `detectors.detect_issues` hands the pass to the controller when the switch is set. Consequences: the switch is invisible to `inspect.signature(TranslationConfig.__init__)`, a caller misspelling it gets a run with repair off rather than a `TypeError`, and the loop cannot run with detection off, which is a dependency rather than a defect -- it has nothing to act on without findings. | A batch is authorised to touch `translation_config.py` and `high_level.py` again, after which the flag becomes a constructor parameter and the loop can take a hook of its own. | B8 |
| W-B8-02 | `spec_check_b8_3.py` assertion 06 | That gate asserted the decision prompt in the working tree equals the last prompt round its own frozen run recorded, which makes the prompt file the property of whichever batch froze it last and breaks the moment a later batch reworks it -- which T8.4c does. The assertion was narrowed rather than deleted: b8.3 now checks that its last round is the prompt its own frozen run actually sent, which is the property that batch can answer for, and the live check that the tree matches the newest round moved to `spec_check_b8_4.py` assertion 03e, which is the batch that owns the file. Every batch that reworks the prompt from now on carries that assertion. | None; the split is permanent. A later batch reworking the prompt moves the live half to its own gate in the same way. | B8.4 |
| W-E1-01 | `.gitignore` one line, outside the batch whitelist `plans/PLAN_E1.md` declares | The repository ignores `*.json` wholesale and un-ignores only `configs/`, `corpus/` and `reviews/`. `docs/eval/results_e1/` is the batch's deliverable and its JSON -- the LOPO fold matrix above all -- is exactly what ledger row B-02 lost by never being written down, so a `!docs/eval/**/*.json` line was added and `spec_check_e1.py` admits `.gitignore` to its allowed files. Nothing else about the ignore file changed. | A plan revision states the results directory's tracking explicitly, at which point the line is ordinary scope rather than a waiver. | E1.2 |
| W-E2-01 | `tools/run_drift_trio.py`,两个 `chain_off` 臂的缓存处置 | PLAN_E2 的 T-E2.1 把两个 off 臂写作 `ignore_cache`,而 `BaseTranslator.translate` 与 `llm_translate` 的 `ignore_cache` 同时关掉读与写:第二臂读不到第一臂的答案(这是要的),但两臂的答案也都不落盘(这是不能要的)——本项目的评估协议是缓存冻结重放,一次无法重放的付费运行是它唯一不允许的东西。两臂改为各自声明一个 cache impact 参数(`e2_r1_arm=off1` / `off2`):`add_cache_impact_parameters` 只进 cache key、不进线上请求,因此两臂发出的 prompt 逐字节相同、谁也服务不了谁、两臂都可零成本重放,而且『两臂 cache_key 不同』成为可断言的事实(`spec_check_e2.py` 断言 02)——那正是同一份 PLAN 的门禁要求的。`chain_on` 臂不声明该参数,留在共享命名空间里冻结重放。 | 一份计划修订把独立采样的手段写成缓存命名空间而非 `ignore_cache`,此后这是普通实现而非豁免。 | E2.1 |
| W-E2-02 | `spec_check_e0.py` assertion 05 | 该断言要求台账"至少有一条未关行",理由是一份**一条都不剩**的台账多半是状态词表停止匹配的假象。batch-e2.2 把最后三条关掉(A-09 与 E-12 重述、E-09 按 GAP-04 声明),这个前提就此不成立:零未关行是本项目能到达的状态,而不是解析故障。断言被收窄而非删除——改为断言**每一行都携带词表里的某个状态词**,词表停止匹配时全部行会落入"未分类"而仍然失败,少了"必须有人未关"这一条。两个方向的闭合(未关行须在缺口登记里有答案、缺口登记提到的行号须在台账里存在)原样保留。**同一批次内该断言被就地升级,净强度高于收窄前**:追加(1) 台账引用的每个 `GAP-xx` 必须在缺口登记里有对应小节(此前只查反向);(2) 台账自述的状态计数、分组行数与合计必须与逐行重算一致——这一条恰能抓住本会话差点留下的陈旧汇总表。 | 无;这是永久收窄。日后若又出现未关行,断言的其余部分照旧管住它。 | E2.2 |
| W-E2-03 | `CLAUDE.md` 两条约定,超出 `plans/PLAN_E2.md` 声明的批次白名单 | 本批次撞上两件只有写进 CLAUDE.md 才不会再犯的事:(1) `batch-e2.1` 整个会话的产物**从未提交**,直到本会话开工才发现,只能随本次入库——§5 原文只说"每批次一个 commit + tag",没说会话粒度;(2) 人工裁决文件填好后仍会被导出它的工具覆写(`splice_manual_review.json` 若非本会话加了 `is_ruled` 就会被下一次 `--offline` 重放抹掉),而 §4.9 只讲 HITL 是可选层、没讲裁决文件的所有权。两条各自加为 §5.11 与 §4.12。门禁 `spec_check_e2.py` 的 `ALLOWED_FILES` 相应加入 `CLAUDE.md`。 | 一份计划修订把 CLAUDE.md 列入批次白名单,此后这是普通范围而非豁免。 | E2.2 |
| W-B9.2R-01 | `spec_check_e0.py` assertion 07b | 该断言要求"没有任何 `spec_check_b*` 门禁排在 E0 之后",它保护的命题是**引用方向**:E0 清点各批次留下的产物,故须在它们之后跑。本批次的门禁 `spec_check_b9_2r.py` 恰好反向——它把 e1/e2 当子进程跑并读它们的退出码与输出,自己不产出任何 E0 引用得到的产物,因此必须排在 E0/E1/E2 之后。断言被收窄而非删除:新增显式例外表 `AFTER_THIS_GATE`,只列该门禁并在注释里写明理由,其余 `b*` 门禁照旧必须在前。 | 无;例外表是永久机制。日后再有"读 E0 而不供料 E0"的门禁,同样逐个列名并写理由,不得改成模式匹配。 | B9.2R |
| W-B9.2R-02 | `spec_check_e2.py` assertion 05a | 该断言原文是"台账不得再有任何未关行",前提是 batch-e2.2 关掉了最后三条(见 W-E2-02)。本批次按用户裁定把四行(A-26/E-02/E-06/E-13)降级为 `artifact pruned, sha recorded`,未关行重新出现——**这是本项目能到达的合法状态**:产物被保留策略淘汰而引文存活,重跑得不到原件。断言被收窄而非删除:E2 自己关的行(A-09/E-09/E-12 与 A-12、A-18~A-24、C-06、G-09)仍逐条断言必须可引,其余每一条未关行必须在 `docs/eval/gap_register.md` 里被点名。**缺陷不是"有未关行",而是"没人写下理由"**,收窄后的断言抓的正是后者。 | 无;这是永久收窄。日后新增未关行一律须在缺口登记里有小节,否则该断言仍然转红。 | B9.2R |
| W-B9.2R-03 | `configs/output_retention.json` 整份退出 gate cache 指纹 | `spec_checks/artifacts.py` 的 `FINGERPRINT_EXCLUDED_KEYS` 原只排除少数"到不了产物"的键(端点、凭据变量名、超时、缓存自身上限)。本批次把输出保留策略的**全部**键列入:该文件只在一次 sweep 结束之后决定从 `examples/output/` 删什么、归档什么,任何流水线路径都不读它,故其取值不可能改变任何产出产物。不列全部键则每次收紧保留策略都要重建整个 gate cache(约一小时),这会让"该收紧就收紧"变成有成本的决定——而本批次的成因恰恰是保留策略没被及时收紧。**代价**:日后往该文件加新键若忘记同步这份清单,指纹会变一次(表现为一次全量重建,不是错误结果)。 | 一份计划修订把"指纹只覆盖流水线可读的配置"写成显式规则,此后按文件而非按键声明,这条即成普通实现。 | B9.2R |
| ~~W-B10-01~~ 已失效(F3) | 逐批验证范围,适用 b10.1..f3 | 五日修复周期把六样张全量回归集中到 F3:期间每批只跑受影响样张的目标页(b10.1 的四样张十四页),证据为局部 PDF 与目标页 PNG。理由是本周期每一批都只动排版后/翻译后的确定性后处理,翻译请求全部命中缓存,而全量六样张一跑约一小时——把它按批重复五次买不到任何本周期回答不了的问题。**代价**:某批改动在未跑样张上的回归要到 F3 才会暴露。 | **已满足**:F3 当日六样张 41 页全量回归完成,两臂各一跑,请求 413 / 命中 413 / API 调用 0;代价所指的"未跑样张上的回归"实际未出现,本周期在成品上引入的新异常为零(F3 报告主表二)。 | B10.1 |
| ~~W-B10-02~~ 已失效(F3) | 每批单门禁脚本与断言分类,适用 b10.1..f3 | §5.3 要求每批门禁含正向/负向/端到端三类断言,本周期额外约定断言只限四类各≥1(锚点正向、范围负向、守恒、端到端),重放矩阵免。理由同 W-B10-01:一个微批次的门禁若要重建重放矩阵,门禁本身比改动贵一个数量级。 | **已满足**:F3 的门禁 `spec_checks/spec_check_f3.py` 按 §5.3 全量写,含正向、负向、端到端三类,并含本周期五个批次的重放矩阵断言。 | B10.1 |
| ~~W-B10-03~~ 已失效(F3) | `docs/eval/results_*` 只读,适用 b10.1..f3 | 本周期不重算任何 E 系列评测产物,门禁只对它们做存在性与哈希校验。这是 CLAUDE.md §4.13 的重述而非放宽,写在这里是因为本周期的改动会改变译后 PDF,而 E 系列的冻结证据是在旧渲染下取的——两者不再可比,重算等于覆写冻结证据。 | **已满足且转为常设**:周期结束,而"冻结证据只读"本就是 §4.13 的条款,不随本周期失效;本行关闭的只是"本周期不重算"这一临时安排。E 系列产物在 F3 后仍不可重算,理由不变(渲染已变)。 | B10.1 |
| ~~W-B10-04~~ 已失效(F3) | run_all 执行范围,适用 b10.1..f3 | run_all 按 SPEC_NO_NESTED 线性执行全部历史门禁,其中若干 b9.x 门禁在"逐批全量跑"纪律下内部重跑完整流水线,单次合计逾两小时(b10.1 实测:第三个此类门禁运行至 137 分钟,log 存 examples/output/b10_1/profiling/)。本周期将 spec_check 二分为 fast(桩驱动/冻结证据,分钟级)与 sweep(内部重跑流水线)两集,逐批 tag 的门禁条件 = 本批 spec_check + fast 集全绿;sweep 集整体推迟至 F3 当日执行一次。代价:sweep 集断言的回归在批间不可见,最迟 F3 暴露。 | **已满足,且代价如期兑现**:F3 当日 sweep 集 18 档补跑完成,暴露两条本周期引入的回归(b0 的 CJK 断言、b1 的消费者名单),两条都在 F3 当日关掉而非豁免。fast/sweep 标注与 `--set` 参数为永久基础设施,保留。 | B10.1 |

## B10 修复周期的迁出登记(F3)

`W-B10-05` 起的十条各自记着一件**永久性内容**——一条断言此后断什么、一个检测器议
题、一个按裁定接受的 scoped gap——而豁免登记是按**失效条件**组织的表。没有失效条件
的内容留在这张表里只会永远失效不掉,所以 F3 把它们迁到各自的常驻处。**原编号保
留**:`spec_check_b10_3.py` 的注释与两份批次报告按 `W-B10-05` 的写法互相引用,改号会
把这些引用打断。

| id | 原内容 | 迁往 / 处置 |
| --- | --- | --- |
| W-B10-05 | `spec_check_b9_2.py` 04a 的断言扩写(结构键 + 两张 by_target 表) | `docs/reports/assertion_contracts.md` **AC-01** |
| W-B10-06 | `spec_check_b9_5.py` 01e/04f 的断言重指(碰撞动作由"恒不写入"改为"不动等大的一对") | `docs/reports/assertion_contracts.md` **AC-02** |
| W-B10-07 | T3 的落点在 `babeldoc/magazine/short_unit.py` 而非上游地板的读取点 | **关闭**。它不是对本项目约定的偏离:CLAUDE.md §3 的"最小化上游文件改动"与该计划所写的落点冲突时,约定优先于计划措辞。事实本身记在 `examples/output/b10_4/b10_4.report.md` §4 与 §11 |
| W-B10-08 | 改动面超出 PLAN_B10_4 括号内枚举的五个 magazine 模块 | **关闭**,按其自身条款("括号内枚举按目录读即可,此后不再登记") |
| W-B10-09 | 新规落在 CLAUDE.md §5.13 而非计划所写的"§6" | **关闭**,按其自身条款(条款已落地,编号以文件为准) |
| W-B10-10 | 碎片检测器对行结构记录块的过标记 | `docs/eval/gap_register.md` **GAP-28** |
| W-B10-11 | 未实现 CJK 字形人名收割器(零案例,按裁定接受) | `docs/eval/gap_register.md` **§7b 缓建项** |
| W-B10-12 | `spec_check_b10_3.py` 01f 的断言扩写(结构键 + 标识符形状) | `docs/reports/assertion_contracts.md` **AC-03** |
| W-B10-13 | `spec_check_b10_5.py` 03f 的断言重指(负向对照改一般形式) | `docs/reports/assertion_contracts.md` **AC-04** |
| W-B10-14 | `spec_check_b10_5.py` 04b/04c 的断言重指(亚像素位移不做逐字节裁剪比对) | `docs/reports/assertion_contracts.md` **AC-05** |

迁出之后,本周期在本表中只剩 `W-B10-01` 至 `W-B10-04` 四行,四行按各自的失效条件在
F3 当日关闭(见上)。**关闭不是删除**:一条关掉的豁免仍要能被读到,否则"我们从来没
有放宽过这一条"会成为可能的读法。

## B11.1

| id | 内容 | 理由 | 失效条件 |
| --- | --- | --- | --- |
| W-B11-01 | 验证范围限 FD-en-v2 单样张:不跑 sweep、不跑其余五样张 | 用户裁决。本批三项改动的证据面全部落在 FD | 下一次全量跑绿。**代价须显式记住**:T1 与 T2 都是上游行为变更,影响面是全语料,其余五样张的回归要到那次全量跑才暴露 |
| W-B11-02 | F2/F3 可比性不再保全 | 用户裁决,论文不使用 F2 对照。人名档切换即换 system prompt,FD 的全部请求重采 | 永久 |
| W-B11-03 | 改动面超出 PLAN_B11_1 的负向范围,多出三条路径:`spec_checks/spec_check_b10_4.py`、`spec_checks/spec_check_e0.py`、`docs/reports/archive/b10_4.zip` | 三处都由本批自身的存在造成,非计划外的顺手改动。(a) e0 断言"每个 b 门禁都排在自己前面",新门禁一出现即为假,按既有惯例记入其 `AFTER_THIS_GATE`;(b) b10.4 的两条断言把"当前选中的人名档"当常量,T3 一改即为假,按 AC-09/AC-10 重指;(c) 本批目录一出现,保留策略即淘汰 b10.4 并按 §4.13 打包 `b10_4.zip`,该归档必须入库,而 b10.4 的三条断言随之改为 SKIPPED(AC-11) | 永久。三处都是一次性的结构调整,不是放宽的阈值 |

**关于 W-B11-03 (c) 的一句补记**:b10.4 的完整译后 PDF 与全部 working dir 在本批
第一次 `run_all` 结束时被保留策略淘汰,已按 §4.13 打包入 `docs/reports/archive/b10_4.zip`
(132 个成员,小件 JSON)。大件(PDF、28 MB 的 checkpoint)超过 `archive_max_file_kb`
仍被删除,**不得以重跑顶替**。受影响的是 b10.4 门禁的三条断言,现按既有的 b10.1 /
b10.3 写法报 SKIPPED 并指名缺失路径。

## B11.2

| id | 内容 | 理由 | 失效条件 |
| --- | --- | --- | --- |
| W-B11-05 | 基线由 f3 重指 `examples/output/b10_5/<样张>/on`;恒等暴露数为 `transliterate` 档下的**下界估计** | f3 会话自愿删除两臂 work/ 与 out/(约 4.5 GB),归档里没有 F3.zip,f3 不能再作基线。b10_5 on 臂的开关组合与 f3 一致,且其 FD 残留集与 f3 **逐条相同**(18 条、id 与 excerpt 列表完全一致),等价性成立。但 b10_5 跑在 `person_names = transliterate` 下而 b11.1 已切 `translate`,恒等段集合可随档变化,故 T2 的恒等暴露计数是下界 | 下一次留有工作目录的全量跑 |
| W-B11-06 | 跑批范围由 T2 的暴露探测**用证据决定**而非预先声明:实跑五样张,Courier-zh 因两项暴露均为 0 不跑 | b11.1 的上游变更影响面未知,用证据收敛优于凭声明缩减。范围裁定可核验:门禁断言"实跑集合 == 探测名单" | 本批结束 |
| W-B11-07 | T4 的验收标准由"已 SKIPPED 的断言恢复为真实执行"改记为"**不可恢复 + 转入缺口登记 + 前瞻性修法 (a)(b)(c) 照做**",八条维持 SKIPPED | 用户裁决(b11.2 续跑指令第 1 条)。实测:八条断言所读的 19 条路径中,工作区或归档存在的只有 1 条,且只是某条断言所需两条中的一条;归档条件(`archive_patterns` 无 `.xml` 与 PDF、`archive_max_file_kb = 2048` 而 checkpoint 为 MB 到几十 MB)使这些证据**从来就进不了归档**。§4.13 禁止以重跑顶替。逐条矩阵见 `examples/output/b11_2/t4_recoverability.json`,缺口见 GAP-31 | 永久。八条已失的断言即便回溯改写也回不来 |
| W-B11-08 | CLAUDE.md 的改动面由"仅新增单臂默认一条"放宽为**新增三条**(§4.14 单臂默认、§4.15 会话自删产出前须先归档或登记豁免、§4.16 门禁证据提取) | 用户裁决(b11.2 续跑指令第 2 条)。T4(d) 与新增的 T4(e) 都是长期约定,自然落点是 CLAUDE.md §4;写在别处会让"这条规矩存在过"难以被读到 | 永久 |
| W-B11-09 | §4.16 的门禁证据提取规矩**只对新门禁生效**,既有门禁的回溯改写不在本批做 | 用户裁决(b11.2 续跑指令第 1 条)。回溯要求逐条判定每条断言真正需要的那几个数并补提取器,作用面是 b1~b11 全部门禁 | 回溯完成时;缺口登记见 GAP-31 的"未补部分" |

**关于 T1 判定口径的一条补记**:同一段落在 `source_audit.report.json` 中记为 `p5#5`、
在 `issues.json` 中记为 `p5#6` —— `pN#k` 是段落在其页内列表中的位置序号,只在**产出它的
那一个 stage 内**稳定。故 T1 的判定表一律按 `debug_id`(同一次运行内)或**文本**对齐,
禁止按 `pN#k` 跨 sidecar 配对。这不是豁免而是口径,登记为 GAP-32。

## B11.3

| id | 内容 | 理由 | 失效条件 |
| --- | --- | --- | --- |
| W-B11-10 | 改动面超出 `PLAN_B11_3.md` §7 一条:`configs/output_retention.json` 纳入本批声明面,登记 b10.5 on 臂的 **12 条 stage-06 checkpoint** 为受保护路径 | 用户裁决。本批 T1/T2 读的正是 `checkpoint.06_styles_and_formulas.*`——formula 标注写在那一档,它是唯一能说清"为什么这条 composition 被标注"的 stage——而 b11.2 的 T0 只登记了 09/11 与 `translate_tracking.json`。中期修订三条件全占:**(a) 只涉尚未执行的路径**(淘汰自 b11.2 T4(c) 起需显式请求,至今未发生,尚无证据丢失、无断言依赖旧面);**(b) 有显式裁决记录**(`examples/output/b11_3/midcycle_revision.json`,REV-B11-3-01);**(c) 只收紧不放宽**(登记受保护路径只会**减少**策略被允许删除的文件;`keep_recent_batches` / `archive_patterns` / `archive_max_file_kb` 一律未动,**归档上限未抬**,b11.2 的那次拒绝原样成立)。"读的东西不受保护"正是 GAP-31 已经让本项目付出八条断言的那个形态,不等它再咬一次 | 该批次交付且无人再指向 b10.5 时,按 b11.2 为该类条目写下的退出条款移除 |
| W-B11-11 | 修法落点由 PLAN 候选表所写的 `StylesAndFormulas 的标注条件` 订正为 `formular_helper.is_formulas_font` 的 broad 模式 | 用户裁决第 (3) 条:「若你判断落点实际在 `formular_helper.py` 而非 `styles_and_formulas.py`,以代码为准记录,PLAN 表里的候选名称按实际落点订正即可——那张表写的是意图,不是路径」。`styles_and_formulas.py:445` 的字体分支本身只是一次集合成员判断,做判定的是 `formular_helper.is_formulas_font`;在 445 处改会把一个字体名问题写成一个分类器问题 | 永久 |
| W-B11-12 | `configs/output_retention.json` 再登记 b10.5 on 臂的 **12 条 stage-05 checkpoint**(`checkpoint.05_paragraph_finder.{json,xml}` × 六样张)为受保护路径 | `PLAN_B11_4.md` T0 直接指派,做法与退出条款照抄 W-B11-10:**逐文件列举、不登记整目录、不抬 `archive_max_file_kb`(仍为 2048)、不改 `archive_patterns`**。stage-05 是**公式尚未存在的最后一档**,本批 T2 的 (c) 栏以它为唯一可用输入——正因读了它,才查明 FD-en-v2 p4#3 与 Vogue-en p1#0 的错序 rail 在标注之前就已存在,从而把 (c) 正确地计为 1 而不是多。该形态即 GAP-34,与 GAP-31 同款,而 GAP-31 已让本项目付出八条断言 | 该批次交付且无人再指向 b10.5 时,按 b11.2 为该类条目写下的退出条款移除 | b11.4 |
| W-B11-13 | 改动面超出 `PLAN_B11_4.md` 的**批次意图**(未超出其声明面——`spec_checks/` 在允许集内):`spec_check_b11_1.py` / `b11_2.py` / `b11_3.py` 三个既有门禁中的 **14 行 CJK 字面量**改写为等价 `\uXXXX` 转义 | 本批**首次跑全量 `run_all`**(316.7 分钟)暴露:`b0` 的 09、`b1` 的 09d、`b2` 的 11c 三条断言禁止 `spec_checks/*.py` 出现 CJK,三者**同属 sweep 集**,而 W-B10-04 之后的各批只跑 fast 集,故 b11.1 起引入的违规**无人看见**——这正是 W-B10-04 自己写下的代价「sweep 集断言的回归在批间不可见」。处置照 F3 的先例:该批当日 sweep 暴露同类 CJK 回归时**当场关掉而非豁免**。改动为**纯编码变换**:转义字符与原字符逐一相同,`check_06i` 以 AST 取出两侧的中文字面量集合逐一比对并断言其未丢失,三个门禁复跑 17/17、32/32、29/29 全绿 | 永久(转义为该目录的既定写法,`spec_check_b1.py` 的 `CJK_RANGES` 注释即其出处) | b11.4 |
| W-B11-14 | **违反 CLAUDE.md §4.13**「冻结产物一经淘汰不得以重跑顶替」:`examples/output/b11_2/FD-en-v2/FD-en-v2.b11_2.pdf` 被 b11.4 的淘汰删除后,以 `run_b11_2.py --only FD-en-v2` 重跑补回 | **用户显式裁决**。该选项在被选中前已逐字写明它违反 §4.13、且会使被淘汰产物与原件不可分辨;用户仍选此项,即为覆盖。执行时以三件事保住可分辨性:原件 sha(`9c0f463a…`)与重生成 sha(`fce48d06…`)**并列入库**(`b11_2_regeneration.json`),b11.2 的台账条目加 `artifact_state` 注记,原件残存物(run.json / conservation.json / 九张 raster)先快照进 `examples/output/b11_4/b11_2_pre_regen_snapshot/`。重跑 **0 次 API 调用、86/86 缓存命中**,内容推导相同,但 PDF 写出不可位复现,故字节不同——**这正是 §4.13 视重跑为另一件产物的理由**。更要紧的是:重跑产出的是**今日之树**的 FD,`check_06d` 因此由「无法执行」变为「确实为假」,暴露 GAP-36。**该禁令被这次违反反证为对的** | 永久(不可撤销:原件已不存在) | b11.4 |

## B11.5

| id | 内容 | 理由 | 失效条件 |
| --- | --- | --- | --- |
| W-B11-15 | 改动面超出 `PLAN_B11_5_REV2.md` 的负向范围一个文件:`babeldoc/format/pdf/high_level.py` 两行(一行 import、一行调用) | T3 的窗口由计划自己指定为「翻译之后、Typesetting 之前(与 paren_dedup 同窗)」。该窗口位于 `_do_translate_single` 主干上,magazine 侧**没有任何既有钩子够得到它**(`hitl.after_translate` 在译后但不持有 `docs`,`paren_dedup.apply` 是那一段唯一的 magazine 调用)。负向范围列了 `indent_policy.py` 却没列调用它的地方,即计划内部的一处不自洽:照列表执行会交付一个**无人调用的 pass**,那不是收窄而是空转。取两行最小改动,理由与 b10.1 为 `paren_dedup` 写下的登记同款。开关 `magazine_indent_policy` 缺省为假,故抬起开关前全流水线逐位无差异,这一点由门禁的回落断言实测 | 永久 |
| W-B11-17 | 改动面再超出负向范围三个既有门禁:`spec_check_b7_5.py`、`spec_check_b9_5.py`、`spec_check_b11_4.py` | T1 与 T4 都由计划指派往 `reviews/FD-en-v2.decisions.json` 写入裁决行,而该文件的**摘要被三处钉住**。CLAUDE.md §4.12 明写:裁决哈希钉只锚定"机器零改动",用户更新时**钉随重钉并留变更记录,不构成门禁失败** —— 所以重钉不是绕过断言,它就是该条款指定的动作。`b7_5.TRUTH_DIGESTS` 与 `b9_5.FD_RULING_DIGEST` 按既有 b9.1 / b9.2 / b11.2 三次重钉的写法原样重钉(was/now/what/who/why 五行)。`b11_4.check_06b` 另作**重指**(AC-17):它此前比对**整份文件**的摘要并与自己批次的冻结件对照,那既会在下次裁决时必然为假,又说不出它真正关心的那一行的任何事;现改为断"`Masthead` 行仍在"加"盘上文件等于 `b7_5` 的钉",把"有没有机器私自改过"这个问题交回带变更记录的那个钉 | 永久 |
| W-B11-16 | `configs/indent_policy.json` 的 `indent_em` 是**钉而非旋钮**:它声明的 4 与 `typesetting.py` 里 `current_x += space_width * 4` 的 4 是同一个数,但本 pass 不消费它,单改此处不改变任何页面 | 计划要求「`indent_em` 旋钮(默认 4 半角宽 = 现行为,带范围)」,同时要求「消费端(上游 typesetting)零改动」。两条同时成立时该键必然不被消费——写成能调的旋钮就是谎。故按其真实身份登记:config 的描述明写它是钉,门禁 `check_04f` 以源文读回 `typesetting.py` 的乘数并与之比对,使这个数一旦分叉当场为红。真旋钮需要动上游,不在本批范围 | 上游消费端接受一个声明的缩进量时 |

**关于 T2 执行序的一条补记**:计划的 T2 序为「谓词先冻结 → 离线测量 → 消费者清单 →
修法」。实际执行为「谓词冻结并哈希入库(`t2_predicate_freeze.json`)→ **写修法** →
离线测量 → 消费者清单」。理由:忠实的测量要求**用真实的 stage 跑两遍取差**
(`t2_measure.py` 驱动 `StylesAndFormulas.process_page` 本体,豁免臂/抑制臂各一遍),
而这需要修法已在树上;先离线重实现一遍谓词再测,测的就是重实现而不是将要发运的代码,
b11.3 的教训正在此。冻结在**任何测量之前**完成且哈希入库,该序保住的是"旋钮不为使数字
成立而动"这件实质,不构成 §5.14(c) 所禁的放宽。这不是豁免而是执行记录。

## B11.6

| id | 内容 | 理由 | 失效条件 |
| --- | --- | --- | --- |
| W-B11-18 | 改动面超出 `PLAN_B11_6.md` 的负向范围两个文件:`babeldoc/magazine/taxonomy.py`(两个常量各加一个键名)与 `tools/column_continuity.py`(改为 import 共享函数) | 两处都是计划自身的要求所蕴含、而列表漏列的。前者:T1 的页级闸由计划指定为「policy 键 `indent_eligible`」,而 `taxonomy.parse_taxonomy` 对 policy 的**未知键一律拒绝**——不把键名登记进 `OPTIONAL_POLICY_DEFAULTS` 与 `OPTIONAL_BOOLEAN_POLICY_KEYS`,`page_types.json` 连载入都做不到,照列表执行交付的是一份加载即报错的配置。前置 4「`preserve_line_structure` 先例」指的正是这条登记路径。后者:T2.1 明写「列带推导复用 `column_continuity` 已验证的那套(**同源函数,不复制代码**)」;共享函数落在 `chain_signals`(在范围内),而「不复制代码」只有在工具改为 import 它时才成立,否则树上留着两份同样的推导,正是该括号禁止的形态。两处改动均以实测背书:重构后的工具在同一 stage 上与重构前**逐行逐字段相同**(107 对、24 判连不变),该比对记在批次报告 §T2 | 永久 |
| W-B11-19 | 改动面再超出负向范围六个既有门禁:`spec_check_b4.py`、`spec_check_b11_2.py`、`spec_check_b11_5.py` | 三条都是**被本批推翻的命题**,按 CLAUDE.md §5.14 与 `docs/reports/assertion_contracts.md` 的既有处置改写并登记为 AC-18 / AC-19 / AC-20,不是为使本批变绿而放宽。b4 的两条:链的成员此后可同页(栏界链),且「判连」与「成边」自本批起是两件事(独占装配);b11_2 的一条:它以字符串比对断工具复用了检测器的候选走查,而那个走查已并入`page_columns`,witness 随之改指并加严;b11_5 的一条:它的 stub 页无 `page_kind`,而页级闸对无 kind 的页判不合格,故 stub 补声明一个由词表自身选出的合格 kind。第四个是 `spec_check_b1.py`:它的断言 06 维护一份**授权消费者名单**,凡提到九个 IL 新字段之一而不在名单上的文件即为越权。页级闸使 `indent_policy.py` 读 `page_kind`,b11.5 与 b11.6 两个门禁各建一张带 kind 的页,三者据此入名单并各附理由 —— 这是该名单自 B4 起每批的既有增长方式(b10.3 / b10.5 的两条写法与此逐字同款),是数据扩充而非命题变更,故不另开 AC 条目。第五个是 `spec_check_b5.py`:它的 13a / 13b 以「升级理由集合恰等于一个」为断言,而栏界链使夹具文档由一条链变七条,其中一条带占位符、走的是站在最前面的那道闸,理由集合遂有两个元素 —— 按 AC-21 收窄为「⊆ {钩子理由, placeholder_bearing} 且必含钩子理由」并**另加一条源码次序断言**为其背书。第六个是 `spec_check_e0.py`:它的 07b 要求**每个批次门禁都排在 e0 之前**,例外由其自身的 `AFTER_THIS_GATE` 名单声明,而b11.1 / b11.2 / b11.3 早已在该名单上 —— 本批把 `spec_check_b11_6.py` 按同一写法加入,是该名单的既有增长方式。另,b11.5 的 02d 按 AC-22 重指 | 永久 |


## B11.7

| id | 内容 | 理由 | 失效条件 |
| --- | --- | --- | --- |
| W-B11-20 | 改动面超出 `PLAN_B11_7_REV2.md` 的负向范围三个 react 文件:`babeldoc/magazine/react/config.py`、`react/actions.py`、`react/controller.py`,外加 `configs/repair_actions.json` | 三处都是**用户裁决**直接要求的。裁决把旋转残留的闭合路线定为「既有孤行动作 + 旋转车道」,并明确修正:受理面不按标签加 `abandon`,改为**增加受理条件 `paragraph.vertical`**。落实这一条必然触及三处:`config.py` 登记键名并放行布尔项(applicability 原本一律按数字校验,布尔项无区间可声明),`actions.py` 的 `admits` 加第二类管辖并改写其文档串,`controller.py` 一行把段落引用告知车道以便记账。`configs/repair_actions.json` 是该条件的声明处。负向范围写的是 `babeldoc/magazine/`,react 在其下,故严格说未越界;登记于此是因为 REV2 的括号清单未点名 react,而这三处的改动性质是**行为变更**而非配置扩充 | 永久 |
| W-B11-21 | 改动面超出负向范围两个既有门禁:`spec_checks/spec_check_b11_6.py`(01e,AC-23)、`spec_checks/spec_check_e0.py`(`AFTER_THIS_GATE` 加一行) | 前者是**被本批推翻的命题**:T1 使 `indent_policy` 成为 `first_line_indent` 的唯一写者,不合格页从"不动"变为"判为不缩进",故 01e 的中间项按 AC-23 重指(结论项即那个标志本身,一字未改)。后者是 `AFTER_THIS_GATE` 自 b9.2r 起每批的既有增长方式(b11.1 / b11.2 / b11.3 / b11.6 写法逐字同款),是数据扩充而非命题变更,不另开 AC 条目 | 永久 |
| W-B11-22 | REV2 的前提校验块与初版**逐字节相同**,其中第 4 条第二分句(「residue 检测器读 IL 段文本,公式字符不计」)已被 REV2 自己的文首第 3 行宣告为伪 | 文件内部不自洽:文首取本批前提差异报告的更正为准,前提块是初版的原样抄录,未随之扫改。执行时以文首为准并照常执行,而非按 §5.2 停止 —— 停止在一条计划自己声明作废的前提上,报告的会是计划的抄录疏漏而不是树与计划的差异。该疏漏记于此,并记于批次交付报告 §0 | 本条不构成豁免,是执行记录;下一份计划若再抄录前提块,须同步扫改 |
| W-B11-23 | 确认性全量 sweep **未跑完**:按用户指令中止于当次门禁(`spec_check_b2.py`)完成之后,不并发、不强杀。§5.8 要求打 tag 前全量 run_all 全绿,本批以此豁免入库,完成权延期至 **b11.8 收口** | 三条理由。(a) **被中止的是一次确认性运行,不是唯一一次运行**:它之前的那一轮全量 sweep 已经做完了 sweep 存在的意义 —— 抓到两处 fast 集够不到的真违规(b0 `09` / b1 `09d`:本批新增代码里有 CJK 三行;b5 `08e`:`chain_backfill` 导入 pipeline,破了该模块自述的纯度),两处**都已修**,并各自以 standalone 复跑复验为绿(b0 exit=0、b1 exit=0、b5 **62/62**)。(b) **中止后的这一轮在中止点上零红**:`spec_check_b0` / `b1` / `b2` 三条 exit=0,记录与日志存 `examples/output/b11_7/sweep_partial/`,按 §4.15 归档而非丢弃。(c) 门禁第 40 条(`05g`)因此转 **SKIPPED-with-cause** 并引本条,而不是在一份不存在的记录上判绿、也不是为一个它没做的决定判红。**诚实界**:`spec_check_b2_1.py` 在信号发出时已经开跑约五分钟(run_all 在一条门禁返回的瞬间即进入下一条,而 sweep 输出带缓冲使守望者晚看到 b2 的结束行),故它是被从中截断的那一条;其 artifact 缓存可能半成,**b11.8 须把 b2_1 当作未跑而非续跑** | **revised@b11.8**：完成权由「b11.8 收口」改为「**性能批**使 sweep 耗时可接受后执行」。用户裁决：b11.8 禁跑全量 sweep（耗时不可接受）。b11.7 第 40 条（`05g`）维持 SKIPPED-with-cause 并引本修订条款。**`b2_1` 的指令随之转给性能批**：其 artifact 缓存可能半成，性能批须把 `b2_1` 当作未跑而非续跑。b11.8 以两类 sweep-only 违规的 fast 集廉价变体（门禁 `07a` / `07b` / `07c`）偿付部分覆盖缺口，sweep 集其余断言的缺口记于 GAP-49。性能批跑完 `--set all` 全绿即失效 |
| W-B11-24 | 改动面超出 PLAN B11.8 负向范围一个上游文件：`babeldoc/format/pdf/high_level.py`（一行 import、一行调用） | 计划把新 pass 写作「双向试装模块（新）」并声明「上游零改动」，但一个排后 pass 需要一个主干调用点，而白名单里没有任何可做挂载点的文件。按 §5.2停工并报告后，用户裁决取 `high_level.py`（而非 `detectors/__init__.py`），理由是位置直白且与 B8 / B10.1 / B11.5 / B11.7 四处同类挂点同形。pass 默认关，开关抬起前逐位无行为差异；已登`UPSTREAM_DIFF.md` 的 B11.8 节 | 永久 |
| W-B11-25 | 执行方未改写 `reviews/*.decisions.json`，而 PLAN B11.8 的 T1 写明「执行方按本裁决改写并重钉哈希」 | 开工前的前提校验发现四条 flatten 裁定已由语料所有者在工作区改为 keep（`examples/output/b11_8/premise_check.json` 第 1 条）。§5.12 规定裁决文件一经填写即归用户所有、机器只读，故本批只按 §4.12 重钉哈希并留变更记录，一字未写。用户已裁定取此路 | 永久 |
| W-B11-26 | 本批 git diff 中出现两个语料真值文件：`corpus/page_labels.json`（内容改动）与 `corpus/registry.user.json`（两条新登记），二者均在 PLAN B11.8 负向范围之外，后者更受 CLAUDE.md §2「任何批次的 git diff 中出现 registry.user.json 即违规」约束 | 两处**都不是机器写的**。批次跑批期间，语料所有者向语料加入两份中文源样张（`Vogue-zh.pdf` / `HuaweiTech-zh.pdf`）：先写 `page_labels.json` 的版面真值，再补 PDF 与 registry 登记。执行方发现后按 §5.2 停工并报告（`spec_check_b7_5` 两红：`05a` 哈希钉、`01b` 裁定未登记样张），用户裁决把这次语料更新归入本批。§2 的例外条款「除非当次 prompt 显式声明用户已更新登记」由该次裁决满足。执行方对这两个文件所做的全部动作是：按 §4.12 重钉 `page_labels.json` 的哈希并留变更记录，以及把两条路径逐个（而非整个 `corpus/`）加入本批门禁的 `ALLOWED_PREFIXES`——逐个是为了让第三个真值文件出现在 diff 里时仍然是一次发现 | 永久 |
| W-B11-27 | 第二处上游改动:`babeldoc/format/pdf/document_il/xml_converter.py`(新增一个 `Converter` 子类与一次注册,共约 20 行含注释),超出 PLAN B11.8 负向范围的「上游零改动」 | 新语料触发的**上游既有缺口**,非本批改动造成:`il_version_1.py:58` 把 `passthrough_per_char_instruction` 声明为 `str | None`,前端在 `can_lazy_render` 分支填进 `LazyPassthroughInstruction`;同一文件的 JSON 写盘早已 `materialize()`,XML 写盘没有,于是持有延迟指令的文档**能写 JSON、写不成 XML**,而 checkpoint 走 XML。`spec_check_b3_3` 因此崩于 `ConverterError`。旧六份样张不进该分支,故缺口至今未暴露。用户裁决:XML 侧补同一动作,与 JSON 路径合流,判例同 b8.4「一个 reader」——两路分歧不存活。三条件均已兑现:(i) 门禁 `07e` 断言含 lazy 字段的桩在两路上materialize 后逐字节一致;(ii) 门禁 `07f` 断言无 lazy 字段的桩 XML 渲染修前修后逐字节相同(修前基准 `7064ffc6…` 在改动前冻结);(iii) `UPSTREAM_DIFF.md` 已逐函数登记 | 永久 |
