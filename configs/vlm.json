{
  "description": "Bounded configuration for the vision model fallback (babeldoc/magazine/vlm_client.py). Same convention as configs/page_features.json: every numeric parameter carries a '<name>_allowed_range' sibling of the form 'low..high', inclusive on both ends. 'enabled' is the master switch and is false by default, so a run performs no model call and no network request unless it is turned on deliberately. 'api_key_env' names the environment variable the credential is read from at request time; the credential itself is never written to any file in this repository. 'base_url' points at an OpenAI-compatible chat completions endpoint. 'token_parameter' and 'temperature' are the transport capability profile: 'token_parameter' is the name the output limit travels under and is one of 'max_tokens' or 'max_completion_tokens', and 'temperature' may be set to null, which leaves the field out of the request body so the service default applies. A model that refuses one of these is a configuration to correct rather than a request to rebuild: the client never retries under a different parameter. Ablation protocol: each model runs at its own supported minimum-variance setting; the setting is part of the cache key and must be reported alongside any accuracy figure. 'model', 'temperature', 'max_output_tokens' and 'token_parameter' shape a reply and therefore enter the cache key, as does 'render_dpi' through the bytes of the page image it produces; 'base_url', 'api_key_env', 'timeout_seconds' and 'max_retries' do not. A parameter sitting at the value every stored reply was already produced under contributes nothing to the key, so declaring a capability does not discard replies already paid for. Anything a reply depends on that this file cannot name is covered by CACHE_KEY_VERSION in babeldoc/magazine/vlm_client.py: raise it to retire every stored reply in one step, which is what to do when the endpoint moves to another provider, or when a model served under an unchanged name is suspected of having changed behind it.",
  "enabled": false,
  "model": "gpt-4o-mini",
  "base_url": "https://api.openai.com/v1",
  "api_key_env": "OPENAI_API_KEY",
  "token_parameter": "max_tokens",
  "temperature": 0.0,
  "temperature_allowed_range": "0..2",
  "max_output_tokens": 512,
  "max_output_tokens_allowed_range": "64..4096",
  "max_retries": 1,
  "max_retries_allowed_range": "0..3",
  "render_dpi": 150,
  "render_dpi_allowed_range": "72..400",
  "verdict_rows": 3,
  "verdict_rows_allowed_range": "1..8",
  "timeout_seconds": 60,
  "timeout_seconds_allowed_range": "5..600"
}
