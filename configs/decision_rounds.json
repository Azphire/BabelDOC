{
  "description": "The deterministic priority of detector kinds offered to the ReAct repair loop. One iteration selects at most the first applicable kind and executes exactly one action in one transaction; every accepted action is re-detected and committed before a later iteration may select the next kind. Every detector kind appears exactly once so an added detector cannot become silently unreachable.",
  "kind_order": [
    "out_of_page",
    "text_text_collision",
    "text_figure_overlap",
    "fragment_cluster",
    "chain_escalation",
    "untranslated_residue",
    "article_ownership",
    "chain_conservation",
    "render_coverage",
    "abnormal_blank",
    "fixed_asset_drift",
    "instruction_compliance",
    "detector_prerequisite_missing"
  ]
}
